# Koradio 视觉差异与裁决记录

> 建立于 VDA-00
> 状态：2026-07-12 的原始裁决保留为历史；2026-07-13 用户尺度校准已取代其中的缩小规格；2026-07-14 用户浏览器反馈进一步校准 Settings 配置页密度并由 VDA-16 首次冻结；2026-07-15 用户验收后的 Radio / Detail 校准已由 VDA-17 重新冻结
> 权威顺序：PRD / User Flow → `design/design.md` → 未覆盖细节对应 PNG

## 2026-07-13 用户尺度校准

用户基于 01–06 HTML 与原型图的 1:1 对比，确认问题不是预览 Fit 缩放，而是早期裁决主动采用了远小于参考图的组件和内容列规格。本次裁决具有更高时效性，并按以下关系取代旧决定：

| 被取代记录 | 新裁决 |
|---|---|
| A-001 | Radio 04–06 改为 `x 72 / w 816px` 固定 rail，并使用新的七个固定 slot；三态仍保持零位移 |
| A-003、C-006 | Tab Bar 改为 `620 × 88px`、五个 `124 × 88px` slot、`88 × 64px` 选中底和 `40px` 图标容器 |
| A-005 | Profile 使用 `848px` rail、`256px` 档案卡、`160px` 头像、`32px` 卡片间距和 `212px` 创建卡 |
| A-007 | 顶部 Logo 改为 `48px`；头像与顶部图标按钮统一为 `64 × 64px`；顶部工具行改为 `64px` 高并锚定 `y 40` |
| B-001 | Profile 使用 `848px` rail、`144px` 上传头像和 `80px` 表单控件；服务异常恢复列保留 `560px` |
| B-002 | 后续 Management 页面使用 `840px` 单列，继续沿用既定结构、材质与圆角规则 |
| B-003 | 后续 Settings 页面使用 `832px` 单列和 `80px` 表单控件，继续沿用既定结构、材质与语义规则 |

本次只修改文字大小、尺寸、边距和排版布局。SVG 节点、图标造型、颜色、材质、圆角、阴影、组件结构、fixture 文案和产品行为全部保持不变。旧条目中的 PNG 测量、当时建议和确认日期仍保留，用于解释本次问题来源，不再作为后续实现规格。

## A 级｜必须确认

### A-001

- 编号：A-001
- 页面与区域：04–06 Radio 主页面骨架
- 差异类别：页面骨架 / 固定 slot
- PNG 表现：三页主要内容宽约 `795–845px`，播放器、队列、DJ 状态栏、对话区、输入和导航纵向位置各不相同。
- 规范要求：Radio rail 固定 `x 200 / w 560`；时间、主内容、队列、DJ 状态栏、`560 × 284px` dialogue well、`x 200 / y 1400 / 560 × 72px` 输入和导航必须共位。
- 严重级别：A
- 推荐裁决：以后续 HTML 严格实现规范固定骨架；仅保留 PNG 的深夜材质、排版气质和内容层级观感。
- 影响页面：04、05、06；并影响 VDA-04、VDA-12、VDA-14。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 `816px` Radio 固定骨架取代。
- 证据截图：[Radio 三态锚点叠图](evidence/vda-00-radio-anchors.png)

### A-002

- 编号：A-002
- 页面与区域：07–08 Detail Sheet 全屏骨架
- 差异类别：页面骨架 / 内容层级位置
- PNG 表现：两图彼此一致，但浅色节目面约从 `y 475` 开始，主内容卡约从 `y 872` 开始。
- 规范要求：浅色节目面 `y 420 / h 1180`；声波区 `y 160 / h 280`；主内容卡 `x 80 / y 760 / 800 × 560px`；节目进度位于 `y 1456`。
- 严重级别：A
- 推荐裁决：保留 PNG 的黑白节目气质和声波观感，HTML 使用规范固定坐标；07/08 只替换状态、歌曲和正文。
- 影响页面：07、08；并影响 VDA-05、VDA-12、VDA-14。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：固定锚点继续有效；主内容卡高度与节目进度宽度已由 A-010 的用户视觉校准更新。
- 证据截图：[Detail 双态锚点叠图](evidence/vda-00-detail-anchors.png)

### A-003

- 编号：A-003
- 页面与区域：一级页面底部 Tab Bar
- 差异类别：主组件结构 / 跨页共享组件
- PNG 表现：各页导航约 `590–620px` 宽、`88–96px` 高，选中底和图标也随之放大。
- 规范要求：固定 `x 300 / y 1504 / 360 × 64px`；五个 `72 × 64px` slot；图标 `24 × 24px`；选中底 `56 × 48px`。
- 严重级别：A
- 推荐裁决：HTML 只保留一套规范导航组件，不沿用 PNG 的放大比例。
- 影响页面：01、04、05、06、09、10、12、13、14、15；并影响所有一级页面与响应式继承规则。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 `620 × 88px` 导航几何取代。
- 证据截图：[底部导航跨页对比](evidence/vda-00-navigation-comparison.png)

### A-004

- 编号：A-004
- 页面与区域：10 Taste 底部导航选中态
- 差异类别：交互语义
- PNG 表现：第二个 Library 图标处于选中态。
- 规范要求：Taste 页面应选中第三个 Taste 图标；导航顺序为 Radio / Library / Taste / Programs / Settings。
- 严重级别：A
- 推荐裁决：HTML 将第三项 Taste 设为选中，不保留 PNG 的错误选中态。
- 影响页面：10；并影响导航状态映射与视觉回归 fixture。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 证据截图：[底部导航跨页对比](evidence/vda-00-navigation-comparison.png)

### A-005

- 编号：A-005
- 页面与区域：02 Profile 档案卡片
- 差异类别：主组件结构 / 组件比例
- PNG 表现：卡片约 `256px` 高，头像约 `160px`，头像成为主要视觉元素。
- 规范要求：卡片推荐 `152px` 高；三列结构使用 `64 × 64px` 头像、24px 内边距与固定操作 rail。
- 严重级别：A
- 推荐裁决：HTML 使用规范的紧凑三列档案卡片；保留 PNG 的低饱和圆形头像与克制边框观感。
- 影响页面：02；并影响 Profile 组件族和响应式代表页 03 的关联校准。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 `256px` 档案卡与 `160px` 头像规格取代。
- 证据截图：[Profile / Programs 组件比例](evidence/vda-00-component-proportions.png)

### A-006

- 编号：A-006
- 页面与区域：12 Programs 节目卡片
- 差异类别：主组件结构 / 内容层级
- PNG 表现：卡片约 `210px` 高，使用大尺寸左侧封面和右侧三张小封面，图像占据较强层级。
- 规范要求：单列节目卡片 `168px` 高、20px 内边距；封面只作为小尺寸、低饱和识别信息，不主导页面。
- 严重级别：A
- 推荐裁决：HTML 使用 `168px` 编辑式卡片，将封面降为辅助识别；保留 PNG 的日期、标题、场景与元数据排版气质。
- 影响页面：12；并影响 Programs 组件族和 VDA-08、VDA-12、VDA-14。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 证据截图：[Profile / Programs 组件比例](evidence/vda-00-component-proportions.png)

### A-007

- 编号：A-007
- 页面与区域：全局品牌 lockup 与顶部工具组件
- 差异类别：共享组件结构
- PNG 表现：Logo、字标、头像、主题/设置/返回按钮在页面间存在肉眼可见的尺寸和间距变化，部分页面组合方式不同。
- 规范要求：Logo `36 × 36px`、字标 `160 × 30px`、头像 `36 × 36px`、图标按钮 `44 × 44px`；同类组件不得页面私有变体。
- 严重级别：A
- 推荐裁决：VDA-02 建立唯一品牌与顶部工具组件；页面只改变位置和允许的元素组合，不改变组件尺寸。
- 影响页面：01–06、09–12、14 以及后续所有使用品牌/顶部工具的页面。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 `48px` Logo 与 `64px` 顶部工具规格取代。
- 证据截图：[Radio 三态锚点叠图](evidence/vda-00-radio-anchors.png)

### A-008

- 编号：A-008
- 页面与区域：14 Settings 偏好设置字段
- 差异类别：内容语义 / 字段命名
- PNG 表现：字段标签显示 `Theme`。
- 规范要求：PRD 和 Prompt 使用 `Theme Mode`，值域为 Dark / Light / System。
- 严重级别：A
- 推荐裁决：HTML 使用 `Theme Mode`；若希望改为 `Theme`，应先更新产品文档中的字段规范。
- 影响页面：14；并影响 Settings fixture、表单标签与验收文案。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 证据截图：[原始参考图 14](../../references/14-settings-config.png)

### A-009

- 编号：A-009
- 页面与区域：07–08 Detail Sheet 歌曲进度与节目整体进度控制
- 差异类别：交互语义 / 主控制结构
- PNG 表现：歌曲进度左侧和节目整体进度右侧各显示一个暂停按钮，形成两个可见播放控制入口。
- 规范要求：PRD、`design/design.md` 与 VDA-05 完成条件均要求 Detail Sheet 只保留一个播放/暂停入口，不显示传统底部播放器控制台；`design/prompt.md` 的歌曲进度按钮为可选项，节目整体进度右侧的 `56 × 56px` 按钮为固定主入口。
- 严重级别：A
- 推荐裁决：歌曲进度只显示时间与细进度线，唯一播放/暂停按钮固定在节目整体进度右侧；不复制 PNG 的双控制入口。
- 影响页面：07、08；并影响 VDA-05、VDA-13、VDA-14。
- 最终决定：已确认；用户在 VDA-05 任务中明确要求产品行为服从 PRD，并确认按该方案实施（2026-07-13）。
- 证据截图：[07 自动验收证据](evidence/vda-05-detail-speaking.png)、[08 自动验收证据](evidence/vda-05-detail-lyrics.png)

### A-010

- 编号：A-010
- 页面与区域：07–08 Detail Sheet 顶部声波、节目标题、主内容卡与节目整体进度
- 差异类别：视觉密度 / 固定组件比例
- 初始 VDA-05 表现：顶部为 24 根波形柱且仅 3 根绿色活动柱；标题为 `52 / 58px`；主内容卡为 `800 × 560px`；节目整体进度宽 `680px`，与右侧按钮间距 `64px`。
- 用户反馈：顶部声波密度不足并要求逐根绿白交替；歌词卡纵向长度不足；节目标题偏小导致顶部空旷；底部波形与右侧按钮间距过大。
- 严重级别：A
- 推荐裁决：07/08 共用 64 柱交替绿白波形；标题改为 `60 / 68px`；主内容卡改为 `800 × 620px`；节目整体进度改为 `720px`，与按钮保持 `24px` 间距；不改变其余固定锚点、内容层级和交互语义。
- 影响页面：07、08；并影响 `design/design.md`、`design/prompt.md`、Detail Tokens、VDA-12、VDA-14。
- 最终决定：已确认；用户通过浏览器评论明确要求按上述方向校准，权威规范已先行同步（2026-07-13）。
- 当前状态：波形、标题字号与单一播放控制继续有效；内容列、卡片和底部控制横向几何已由 A-011 的后续用户视觉校准更新。
- 证据截图：[07 校准后自动验收证据](evidence/vda-05-detail-speaking.png)、[08 校准后自动验收证据](evidence/vda-05-detail-lyrics.png)

### A-011

- 编号：A-011
- 页面与区域：07–08 Detail Sheet 浅色节目面内容列、顶部信息区、正文卡片与底部控制
- 差异类别：固定组件比例 / 纵向节奏
- A-010 后表现：节目面内容列左右各留 `80px`；标题、歌曲信息与进度分别位于 `y 500 / 640 / 700`；主内容卡为 `x 80 / y 760 / 800 × 620px`；卡片到节目进度间距 `76px`。
- 用户反馈：卡片内容区左右留白需减少约三分之一；串讲词/歌词卡片再增高约八分之一；顶部文字信息和底部间距需重新压缩与平衡，避免大块空洞。
- 严重级别：A
- 推荐裁决：仅为 Detail Sheet 建立 `x 56 / w 848px` 专属内容列；标题、歌曲信息、歌曲进度与主卡依次调整到 `y 484 / 584 / 636 / 700`；主卡改为 `848 × 700px`；节目进度改为 `x 56 / w 768px`，播放按钮改到 `x 848`，维持 `24px` 间距和左右 `56px` 对称边距；不改变其他页面族内容列。
- 影响页面：仅 07、08；并影响 Detail 专属 Tokens、VDA-12 与 VDA-14，不影响 Radio、Profile、Management、Settings 页面族。
- 最终决定：已确认；用户通过浏览器评论明确限定这些标准仅对 Detail Sheet 生效，权威规范已先行同步（2026-07-13）。
- 证据截图：[07 二次校准自动验收证据](evidence/vda-05-detail-speaking.png)、[08 二次校准自动验收证据](evidence/vda-05-detail-lyrics.png)

### A-012

- 编号：A-012
- 页面与区域：07–08 Detail Sheet 串讲词 / 歌词卡片的已读大号正文
- 差异类别：明确视觉色值 / WCAG AA 文本对比度
- VDA-12 前表现：规范和 HTML 使用 `#A8A7A1`，在 `#F5F3F6` 内容卡上实测仅 `2.19:1`，未达到 29–30px 大号正文所需的 `3:1`。
- 规范要求：已读内容需弱于当前句、强于不可辨识的装饰灰；文字必须满足 WCAG AA，状态层级不得只依赖颜色。
- 严重级别：A
- 推荐裁决：将已读正文统一改为 `#898A86`，在 `#F5F3F6` 内容卡上为 `3.15:1`；当前句继续使用 `#14161A`，未播放内容继续使用 `#767A80`，不改变字号、字重、行高、布局或三层语义。
- 影响页面：07、08；同步 Detail token、`design/design.md` 与 `design/prompt.md`。
- 最终决定：已确认；用户于 2026-07-14 明确确认推荐方案，VDA-13 已完成 HTML 主源和权威规范同步。
- 证据截图：[Reduce Motion 下的 Detail](evidence/vda-13-reduced-motion.png)

## B 级｜按页面族批量确认

### B-001

- 编号：B-001
- 页面与区域：01–03 Profile / Onboarding 页面族密度
- 差异类别：整页密度 / 排版比例
- PNG 表现：01 错误主视觉偏大；03 内容列、头像和输入控件整体大于规范；02 的主卡片结构另见 A-005。
- 规范要求：服务异常 `560px`、Profile `720px`；输入默认最小高 `48px`；头像上传 `88 × 88px`。
- 严重级别：B
- 推荐裁决：统一回到规范内容列和组件尺寸，保留 PNG 的宽松留白与暗色材质。
- 影响页面：01、02、03。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 Profile `848px` rail、`144px` 上传头像和 `80px` 表单规格取代。
- 证据截图：[Profile / Programs 组件比例](evidence/vda-00-component-proportions.png)

### B-002

- 编号：B-002
- 页面与区域：09–13 Management 页面族密度
- 差异类别：整页密度 / 卡片比例
- PNG 表现：内容列约 `825–845px`，目标为 `800px`；列表行、卡片和导航普遍偏大。
- 规范要求：管理页 `800px` 单列，普通卡片 `20px` 圆角，列表按规范行高与内边距统一。
- 严重级别：B
- 推荐裁决：采用规范 `800px` 列与统一卡片规格；保留 PNG 的编辑排版、低亮边框和信息层级。
- 影响页面：09、10、11、12、13；A-004、A-006 优先单独确认。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；内容列尺寸已由“2026-07-13 用户尺度校准”的 `840px` Management rail 取代，其余结构与材质裁决继续有效。
- 证据截图：[Profile / Programs 组件比例](evidence/vda-00-component-proportions.png)

### B-003

- 编号：B-003
- 页面与区域：14–15 Settings 页面族密度
- 差异类别：整页密度 / 卡片比例
- PNG 表现：内容列约 `830–835px`，表单、检测卡片和导航整体比规范略大。
- 规范要求：Settings 内容列 `820px`；输入高 `48px`；检测卡片 24px 内边距；底部导航使用共享固定规格。
- 严重级别：B
- 推荐裁决：使用规范 `820px` 列和表单/卡片尺寸，保留 PNG 的本地工具气质和 TTS 降级层级。
- 影响页面：14、15；A-003、A-008 优先单独确认。
- 最终决定：已确认；采纳推荐裁决（2026-07-12）。
- 当前状态：历史决定；内容列已由“2026-07-13 用户尺度校准”的 `832px` Settings rail 取代，配置页输入尺寸与纵向节奏又由 B-004 更新，其余语义与材质裁决继续有效。
- 证据截图：[原始参考图 14](../../references/14-settings-config.png)、[原始参考图 15](../../references/15-settings-diagnostics.png)

### B-004

- 编号：B-004
- 页面与区域：14 Settings 服务配置页密度与偏好字段辅助文案
- 差异类别：整页纵向节奏 / 表单控件比例 / 辅助信息密度
- 校准前表现：四个服务配置输入框为 `80px` 高；服务状态、服务配置、偏好设置、本地数据、操作区和导航之间的间距从 `4px` 到 `28px` 不等；DJ Voice Style 选择框下重复显示声音风格说明，导致底部区域拥挤。
- 用户反馈：移除声音风格选择框下的小文字；缩小服务配置输入框纵向尺寸；统一页面组件间距并重点释放底部空间。
- 严重级别：B
- 推荐裁决：保留 `832px` Settings rail 与现有内容层级，单独将 Settings 服务配置输入框改为 `64px`，主要区块之间统一为 `24px`，移除选择框下的重复说明；共享 `80px` 输入 token、检测页结构与产品行为不变。
- 影响页面：仅 14；同步 `design/design.md`、`design/prompt.md`、Settings 专属 token、固定 fixture 和 VDA-09 验收记录，不影响 15 及其他页面族。
- 最终决定：已确认；用户通过浏览器评论明确要求按上述方向校准（2026-07-14）。

## C 级｜自动裁决规则

### C-001

- 编号：C-001
- 页面与区域：全局弱阴影、分割线和表面亮度
- 差异类别：微观材质
- PNG 表现：不同页面的阴影、边框亮度和背景颗粒略有差异。
- 规范要求：使用 Design Tokens 的 Surface、Border Subtle 与极弱阴影。
- 严重级别：C
- 推荐裁决：按主题 token 与共享组件统一，不逐页复制 PNG 局部阴影。
- 影响页面：01–15。
- 最终决定：已自动裁决。
- 证据截图：对应原始 PNG。

### C-002

- 编号：C-002
- 页面与区域：封面、头像和装饰图像裁切
- 差异类别：未规定微观细节
- PNG 表现：图像内容和裁切位置各不相同。
- 规范要求：固定比例容器、`object-fit: cover`、低饱和，不抽取主题色。
- 严重级别：C
- 推荐裁决：保留图片气质但使用规范固定比例和居中裁切，不追随随机裁切。
- 影响页面：02、03、04–06、09、12、13、14。
- 最终决定：已自动裁决。
- 证据截图：对应原始 PNG。

### C-003

- 编号：C-003
- 页面与区域：波形具体柱形与纸面颗粒
- 差异类别：随机视觉细节
- PNG 表现：每张图的柱高和纹理不同。
- 规范要求：波形表达状态，常规 PLAYING 使用 12–24 根竖条；Detail 按 A-010 使用 64 根逐根绿白交替竖条；Reduce Motion 下停止动画。
- 严重级别：C
- 推荐裁决：实现共享状态波形规则，不逐像素复刻随机柱高或纹理。
- 影响页面：01、05–08、10。
- 最终决定：已自动裁决。
- 证据截图：[Detail 双态锚点叠图](evidence/vda-00-detail-anchors.png)

### C-004

- 编号：C-004
- 页面与区域：普通间距与卡片内部对齐
- 差异类别：普通间距
- PNG 表现：局部文字、图标、按钮与边框距离有少量不一致。
- 规范要求：4px 基础网格；按卡片类型使用 16 / 20 / 24 / 40px 内边距。
- 严重级别：C
- 推荐裁决：使用共享组件网格；`4px` 内生图漂移忽略，超过 `4px` 且影响整页密度的归入对应 B 条目。
- 影响页面：01–15。
- 最终决定：已自动裁决。
- 证据截图：对应原始 PNG。

### C-005

- 编号：C-005
- 页面与区域：字体抗锯齿、轻微字宽与图标光学校正
- 差异类别：渲染差异
- PNG 表现：生成图文字边缘、字宽和图标中心存在轻微不稳定。
- 规范要求：系统字体与定义字号/行高；图标使用统一线性家族并做视觉中心校正。
- 严重级别：C
- 推荐裁决：以 CSS 字体规格和共享图标组件为准，不逐像素追随生成图抗锯齿。
- 影响页面：01–15。
- 最终决定：已自动裁决。
- 证据截图：对应原始 PNG。

### C-006

- 编号：C-006
- 页面与区域：共享底部 Tab Bar 内部几何
- 差异类别：普通间距 / 固定尺寸协调
- PNG 表现：原始参考图的导航整体明显大于规范，无法作为内部间距基准。
- 规范要求：Tab Bar `360 × 64px`、五个 slot 各 `72 × 64px`、选中底 `56 × 48px`，同时在示例 CSS 中给出 `8px` 内边距。
- 严重级别：C
- 推荐裁决：严格保持总尺寸、slot 和选中底的固定几何；不再让 `8px` 成为增加组件外尺寸的布局内边距，视觉内收由居中的 `56 × 48px` 选中底表达。
- 影响页面：所有使用底部 Tab Bar 的一级页面与组件目录。
- 最终决定：已自动裁决；固定组件几何优先，未改变导航结构、层级或交互语义。
- 当前状态：历史决定；已由“2026-07-13 用户尺度校准”中的 `620 × 88px` 总尺寸、`124 × 88px` slot 与 `88 × 64px` 选中底取代。
- 证据截图：[底部导航跨页对比](evidence/vda-00-navigation-comparison.png)；VDA-02 浏览器几何检查实测 Tab Bar `360 × 64px`、slot `72 × 64px`、选中底 `56 × 48px`。

### C-007

- 编号：C-007
- 页面与区域：Light 核心页 01–08 的环境光与 Radio 表面薄层
- 差异类别：主题材质 / 微观层级
- PNG 表现：原始参考图只有 Dark 状态，无法直接给出 Light 环境光与表面薄层色值。
- 规范要求：Light 使用冷灰白页面背景、白色或近白卡片，并独立校准材料层级，不做 Dark 机械反色。
- 严重级别：C
- 推荐裁决：01–08 的 Light 环境光使用 Light Accent 低透明度变量；Radio 卡片和对话的弱渐变使用主题表面辉光变量；Dark 保持原取值，07–08 保持 Detail 专属固定色面。
- 影响页面：01–08 与共享 Radio 卡片目录；不作用于 09–15 页面校准。
- 最终决定：已自动裁决；未改变布局、内容层级、组件结构或交互语义。
- 证据截图：[VDA-10 Light 汇总](evidence/vda-10-light-contact.png)；[原始 PNG / Light 并排对照](evidence/vda-10-light-comparison.png)。

### C-008

- 编号：C-008
- 页面与区域：Light 管理页 09–15 的环境光、成功 / 警告状态文字与小号错误文字
- 差异类别：主题材质 / 状态可读性
- PNG 表现：原始参考图只有 Dark 状态，无法直接给出 Light 环境光或状态文字色值；Dark 中成功、警告和错误文字依赖高对比深色表面。
- 规范要求：Light 使用冷灰白背景和白色 Surface，状态不得只依赖颜色表达，文字需满足 WCAG AA；页面结构必须与 Dark 保持一致。
- 严重级别：C
- 推荐裁决：09–15 Light 环境光统一使用主题变量；成功 / 警告色保留在状态点、图标和边框，小号状态文字使用 Secondary；错误文字使用 Error 与 Text Primary 的主题混合色加深。Dark 取值和两主题几何保持不变。
- 影响页面：09 Library、10–11 Taste、12–13 Programs、14–15 Settings 的 Light 渲染；System 解析为 Light 时继承相同规则。
- 最终决定：已自动裁决；未改变产品行为、页面骨架、内容层级、组件结构、交互语义或 Dark 视觉。
- 证据截图：[VDA-11 Light 汇总](evidence/vda-11-light-contact.png)；[原始 PNG / Light 并排对照](evidence/vda-11-light-comparison.png)。

### C-009

- 编号：C-009
- 页面与区域：03、05、08、10、14 的 Mobile / Tablet / Desktop 响应式边距、滚动层与固定区域
- 差异类别：响应式微观几何 / 安全区协调
- PNG 表现：原始参考图仅提供 `971 × 1619px` 竖版观感，没有手机、平板或宽屏状态，无法定义三组 viewport 的固定区间距与组件收敛比例。
- 规范要求：响应式分别使用 `20px / 28px / 32–56px` 页面安全边距；Radio 宽屏保持中央单列；Detail 覆盖完整产品画布；管理页与 Settings 保持单列；底部内容预留导航和 safe area；所有可用操作命中区不小于 `44 × 44px`。
- 严重级别：C
- 推荐裁决：Profile 使用可滚动单列表单；Radio 固定顶部品牌、场景输入和导航，仅滚动中间节目内容；Detail 保持上下声场 / 节目面并在宽屏居中 `848px` 内容列；Management 固定顶部与导航并滚动单列内容；Settings 固定顶部、操作区和导航并滚动配置内容。Mobile 按共享比例将顶部工具收敛为 `48px`、导航收敛为 `72px` 高和 `56 × 56px` 选中底，H1 / 大号时间使用规范 `28px / 48px`，不隐藏字段、状态或操作。
- 影响页面：03、05、08、10、14 的响应式代表状态；同族 02–03、04–06、07–08、09–13、14–15 继承对应 shell、滚动、安全区和单列规则。
- 最终决定：已自动裁决；只处理规范未精确给出的响应式间距和比例，不改变产品行为、内容层级、主组件结构、主题气质或交互语义。
- 证据截图：[Mobile Radio](evidence/vda-12-mobile-05-radio-playing.png)、[Tablet Detail](evidence/vda-12-tablet-08-radio-detail-lyrics.png)、[Desktop Taste](evidence/vda-12-desktop-10-taste-overview.png)；同目录另有其余 12 张代表页 / viewport 派生截图。

### C-010

- 编号：C-010
- 页面与区域：01–15 页面切换、主导航键盘移动、Detail Sheet 开合与焦点回收
- 差异类别：交互微观时序 / 焦点路径
- PNG 表现：静态参考图无法定义页面进入、Sheet 开合、方向键顺序、焦点循环和关闭后的焦点位置。
- 规范要求：页面过渡使用 `220ms`；Detail Sheet 使用专属开合节奏，焦点必须可见且顺序与视觉一致，Modal 需限制焦点并支持 Escape；Reduce Motion 下停止非必要过渡。
- 严重级别：C
- 推荐裁决：页面进入使用 `4px` 位移加淡入；Detail 进入 / 退出分别使用 `560ms / 440ms` 与 `160px` 垂直位移，进入使用标准缓动平缓减速停靠，退出向下加速回收。主导航支持左右方向键、Home、End；页面切换聚焦 H1，Detail 打开聚焦关闭按钮，Tab 在两个 Detail 控件间循环，Escape 或关闭后焦点回到 DJ 状态。Reduce Motion 下跳过开合延迟并移除上述动画。
- 影响页面：01–15 共享预览行为，重点影响 05、08、09 与底部导航。
- 最终决定：已自动裁决；2026-07-14 用户浏览器反馈原 `420ms / 320ms`、`48px` 位移在 Fit 缩放下过快且近似闪现，因此改用上述 Detail 专属节奏。只调整视觉连续性与关闭完成机制，不改变页面结构、内容或产品行为。
- 证据截图：[键盘 Focus](evidence/vda-13-keyboard-focus.png)、[Reduce Motion Detail](evidence/vda-13-reduced-motion.png)

### C-011

- 编号：C-011
- 页面与区域：共享控件交互状态、播放波形暂停态与状态播报
- 差异类别：状态可见性 / 非颜色反馈
- PNG 表现：参考图只展示少量静态默认态，无法定义 Hover、Active、Loading、Disabled、错误关联、切换选中与暂停后的持续反馈。
- 规范要求：所有正式交互覆盖 Hover、Active、Focus、Disabled、Loading、Error；状态不得只依赖颜色；加载和错误需适当使用 `aria-busy`、`role=status`、`role=alert` 与 `aria-live`；暂停或 Reduce Motion 时波形停止。
- 严重级别：C
- 推荐裁决：共享控件使用亮度、位移、缩放、透明度和双层 Focus Ring 表达状态；Loading 在保留文字的同时使用三点节奏，Reduce Motion 下变为静态三点；收藏、主题和筛选同步 `aria-pressed` / `aria-checked`；暂停同步按钮名称、`PAUSED` 文字、live region 与静止波形；字段错误通过 `aria-invalid` 和 `aria-describedby` 关联。
- 影响页面：组件目录与 01–15 HTML 视觉主源。
- 最终决定：已自动裁决；状态继续保留明确文字和既有色彩语义，不新增布局层级、真实服务调用或生产行为。
- 证据截图：[键盘 Focus](evidence/vda-13-keyboard-focus.png)、[200% Zoom](evidence/vda-13-zoom-200.png)

### C-012

- 编号：C-012
- 页面与区域：01–15 Prototype 与五类响应式代表页的正式截图捕获
- 差异类别：派生产物稳定性 / 字体渲染微差
- PNG 表现：静态参考图不能定义浏览器捕获时机、设备像素比、动效帧、参考叠层或跨次字体抗锯齿差异。
- 规范要求：HTML / CSS / JavaScript 是唯一主源；PNG 是派生视觉参考；字体抗锯齿不做逐像素追随，布局、尺寸、内容和固定几何必须稳定。
- 严重级别：C
- 推荐裁决：固定使用 `deviceScaleFactor: 1`、Reduce Motion、隐藏参考叠层，并等待字体、图像、`500ms` 布局稳定期与两次 animation frame 后捕获画布；SHA-256 只验证正式文件完整性，复渲染比较解码像素并忽略字体抗锯齿，允许不超过 `0.6%` 的像素差异率。临时 HTTP QA 服务使用系统分配端口，不构成产品运行入口或已确认端口。
- 影响页面：60 张 VDA-14 正式基线及后续视觉回归。
- 最终决定：已自动裁决；未改变布局、组件、内容层级、主题气质或交互语义。
- 证据截图：[Dark Prototype 联系表](evidence/vda-14-dark-contact.png)、[Light Prototype 联系表](evidence/vda-14-light-contact.png)、[Dark 响应式联系表](evidence/vda-14-dark-responsive-contact.png)、[Light 响应式联系表](evidence/vda-14-light-responsive-contact.png)

### C-013

- 编号：C-013
- 页面与区域：11 Taste 编辑页常听风格标签内部
- 差异类别：组件内部微观对齐
- 浏览器表现：用户首次标注 `Dream Pop` 标签内的拖动图标、文字与关闭图标未形成稳定的水平居中关系；固定同尺寸容器后再次复核，关闭 SVG 的光学中心仍略高于左侧拖动图标。
- 规范要求：标签维持现有 `48px` 最小高度、文字层级、图标尺寸与拖动 / 移除语义；未规定的内部微观对齐按组件一致性处理。
- 严重级别：C
- 推荐裁决：标签主轴显式使用水平居中；两侧图标各使用固定 `16 × 16px` 居中容器，消除匿名行盒对视觉中心的影响；仅在 Taste 标签内将右侧关闭 SVG 下移 `1px` 完成光学对齐。标签尺寸、内边距、间距、顺序、文案与交互语义均保持不变。
- 影响页面：11 Taste 编辑页 Dark / Light 及其响应式继承规则。
- 最终决定：已按用户两次浏览器标注完成容器居中与关闭 SVG 光学补正，并同步正式基线（2026-07-14）。
- 证据截图：[Dark 正式基线](../baselines/dark/11-taste-edit.png)、[Light 正式基线](../baselines/light/11-taste-edit.png)

### C-014

- 编号：C-014
- 页面与区域：VDA-15 Figma 文档页、共享组件与页面 Frame
- 差异类别：派生镜像可编辑性 / 字体渲染能力
- Figma 表现：已验收页面以 PNG 图像填充导入时可以完整保留 HTML 主源观感，但不能转换为可编辑 DOM；Figma 可列出 SF Pro 字体，但插件新建 SF Pro 文本在本次环境中产生零宽度度量；响应式媒体查询、CSS `calc()`、动效与交互语义也不能无损映射为静态变量。
- 规范要求：HTML / CSS / JavaScript 是视觉设计资产唯一主源；PNG 与 Figma 均为派生产物，不得为提高 Figma 可编辑性而静默改变布局、组件、内容层级或交互语义。
- 严重级别：C
- 推荐裁决：60 张已验收基线以图像填充 Frame 保真导入；Foundations、文字样式和 7 组共享组件使用原生 Figma 变量、样式与组件表达可复用规则。保留 SF Pro 的变量与文字样式映射，Figma 文档和组件中的可见说明文字使用 Inter，等宽文字使用 Roboto Mono；页面真实字体观感继续由基线图保留。响应式计算与交互限制在文件内显式记录，完整页面—状态—组件映射留给 VDA-16。
- 影响页面：仅 Figma 派生镜像，不修改 HTML 主源、正式 PNG、产品行为或视觉规范。
- 最终决定：已自动裁决；以保真、可追溯和主源单一性优先，不伪造可编辑页面结构。
- 证据：[Koradio · Visual Baseline · MVP](https://www.figma.com/design/ZxAWTQW5aH3VMd9H3T8zcJ)

### C-015

- 编号：C-015
- 页面与区域：VDA-16 页面 / 状态 / fixture / 基线开发交接映射
- 差异类别：追溯粒度 / 非默认变体基线边界
- 当前表现：VDA-14 的 60 张正式 PNG 覆盖 15 个默认页面状态与五类响应式代表页；09–15 另有 20 个非默认固定变体，已由 HTML 与既有自动验收覆盖但没有独立正式 PNG。页面元数据和主要固定内容位于 `pages.js`，部分变体专属状态文案固定在 `app.js` renderer helper。
- 规范要求：VDA-16 建立页面、状态、组件、token、fixture 与基线映射；HTML / CSS / JavaScript 保持唯一主源，PNG 与 Figma 不得反向定义或扩展视觉规格。
- 严重级别：C
- 推荐裁决：在 `handoff-map.md` 中显式列出 35 个固定状态、默认基线覆盖、非默认变体边界及两处 JavaScript 固定内容位置；不为满足文档对称性额外生成未纳入 VDA-14 范围的 PNG，也不在冻结任务中机械迁移 renderer-local 文案。后续前端必须实现并验收全部 35 个状态，不能用默认截图替代非默认状态验收。
- 影响页面：01–15 的开发追溯与视觉验收；不修改 HTML 主源、正式基线、Figma、布局、内容层级、产品行为或交互语义。
- 最终决定：已自动裁决；优先保持已验收资产稳定并完整披露覆盖边界。
- 证据：`design/assets/reports/handoff-map.md`、`design/assets/fixtures/pages.js`、`design/assets/prototype/app.js`、`design/assets/baselines/manifest.json`

### C-016

- 编号：C-016
- 页面与区域：04–06 Radio 时间、主内容与 DJ 对话；07–08 Detail 串讲词、顶部声波与节目整体进度
- 差异类别：冻结后用户视觉反馈 / 排版密度 / 波形表现
- 当前表现：VDA-16 冻结版本中的 Radio 时间使用较小的等宽字体，播放器与主内容 slot 为 `408px`、DJ 对话为 `224px`；07 串讲词在卡片高度内均匀分布，顶部声波较细且含绿色柱，动画以独立垂直缩放为主，节目进度柱高使用规则取模序列。
- 规范要求：用户确认 Radio 时间改用本机 `HONOR Sans Heavy` 并放大；播放器约缩短六分之一且内部节奏统一收敛，DJ 对话约增高三分之一；07 串讲词行距约减半；顶部声波柱需更粗、顶点形成平滑曲线、只用白色与浅灰色，并以波浪方式持续传播；节目整体进度不得过于规则。
- 严重级别：C
- 推荐裁决：Radio 原型时间使用 `80px / 80px / 900`、移动端 `56px / 56px / 900`；04–06 共用 `340px` 主内容 slot、`24px` 播放器内边距与 `288px` DJ 对话 slot。07 串讲词顶部对齐、分句组间距 `24px`。07–08 使用 64 根 `7px`（移动端 `3px`）白灰三档柱体，以确定性 Catmull-Rom 包络生成平滑峰谷，动画周期 `880ms`、相邻柱相位差 `13.75ms`、缩放约 `0.78–1`；暂停和 Reduce Motion 停止。节目进度使用确定性噪声与多频簇组合，避免简单周期重复。
- 影响页面：04、05、06、07、08 及 05 / 08 响应式代表页；同步影响 `design/design.md`、`design/prompt.md`、Radio / Detail tokens、正式基线、Figma 派生镜像与冻结记录，不改变产品行为、页面信息架构、其它页面族或 Figma 原生组件库。
- 最终决定：用户于 2026-07-15 明确“验收通过”；按上述裁决更新权威设计文档与 HTML 主源，重建 60 张正式基线并刷新 Figma 全部 60 个页面 Frame，以 VDA-17 重新冻结。
- 证据：Git `6e97fb74826cdd48e5f75fe57646ac55340aab3c`、`design/assets/baselines/manifest.json`、[Koradio · Visual Baseline · MVP](https://www.figma.com/design/ZxAWTQW5aH3VMd9H3T8zcJ)

## 后续执行约束

1. 核心体验骨架：Radio 按 2026-07-13 用户尺度校准执行；Detail 继续按 A-002 执行。
2. 共享组件：尺寸按本次尺度校准执行；A-003、A-007 和 C-006 只保留为历史依据。
3. 页面语义与未被尺度校准覆盖的主组件结构继续按 A-004、A-006、A-008 执行；A-005 的尺寸以本次尺度校准为准。
4. Profile、Management 页面族尺寸以 2026-07-13 尺度校准为准；Settings rail 继续使用 `832px`，14 的输入尺寸与纵向节奏按 B-004 执行；B-001–B-003 中未涉及尺寸的结构、材质与语义规则继续有效。

后续 HTML 不得以局部页面样式绕过已经确认的裁决；若需要改变裁决，必须先更新对应权威文档和本记录。

## VDA-01 执行记录

- 状态：已于 2026-07-12 通过自动验收并完成。
- 范围：只建立零构建预览壳、15 页固定 fixture、主题与预设 viewport 定位；画布内容是明确标注的骨架占位，不是页面视觉基线。
- A / B 级差异：未发现新增差异；VDA-00 已确认的 A-001–A-008、B-001–B-003 没有被改写或重新裁决。
- C 级裁决：预览壳的弱阴影、分割线和表面层级按 C-001 统一；普通间距使用 4px 基础网格并按 C-004 处理；字体渲染和图标光学校正按 C-005 处理。
- 不适用项：C-002 的媒体裁切与 C-003 的波形细节尚未进入 VDA-01 页面占位，留待对应页面族与组件任务执行。
- 证据：`design/assets/prototype/index.html`、`design/assets/prototype/styles.css`、`design/assets/prototype/app.js` 与 `design/assets/fixtures/pages.js`。
- 自动验收：5 个交付文件、15 个页面状态、3 个主题、4 个 viewport、180 条唯一定位组合和 15 张参考图路径全部通过；浏览器验证代表状态、无效参数回退、参考 PNG `971 × 1619px` 与控制台无错误。

## VDA-02 执行记录

- 状态：已于 2026-07-12 通过自动验收并完成，共享组件确认门通过。
- 范围：建立 `tokens.css`、`components.css` 与 Dark / Light 组件目录，展示品牌区、顶部工具、导航、按钮、输入、卡片、状态、固定比例媒体、播放器、队列、排版、波形和 Focus；不实现 15 页完整视觉。
- A / B 级差异：A-003、A-007 与 B-001–B-003 按 VDA-00 已确认方案执行；没有新增或未关闭的 A / B 级差异。
- C 级裁决：C-001 统一表面、边框和弱阴影，并让 Light 状态色只用于状态点、状态文字使用满足 AA 的 Secondary 文本色；C-002 使用固定比例媒体容器；C-003 建立共享模拟波形并支持 Reduce Motion；C-004 使用 4px 网格和统一卡片内边距；C-005 使用统一系统字体与线性 SVG 图标；新增 C-006 协调底部导航固定几何与内部间距。
- 证据：`design/assets/prototype/tokens.css`、`design/assets/prototype/components.css`、`design/assets/prototype/catalog.html`、`design/assets/prototype/catalog.css`、`design/assets/prototype/catalog.js` 与原型定位器中的目录入口。
- 自动验收：JavaScript 语法与 Git whitespace 检查通过；浏览器验证 Dark / Light 主题切换、7 类目录、15 页定位器回归、无控制台错误、无画布横向溢出、交互命中区均不小于 `44 × 44px`；Light 主题主要/次要文字对背景对比度为 `15.99:1` / `5.27:1`，Dark 为 `18.12:1` / `9.06:1`；实测画布 `960px`、品牌图标 `36 × 36px`、Tab Bar `360 × 64px`、slot `72 × 64px`、选中底 `56 × 48px`。

### 2026-07-13 图标资产更新

- 用户确认采用 `design/assets/icons/` 中的原型 SVG，替换 VDA-02 组件目录内的 CSS 品牌图形与五个临时 Tab 图标。
- `koradio-brand-mark.svg` 与五个 `tab-*.svg` 保持原始 `viewBox`、path、描边、填充和节点结构不变；共享组件通过外部资源引用，只将品牌容器设为 `36 × 36px`、Tab 图标容器设为 `24 × 24px`。
- 主题、播放等其余工具图标不在本次替换范围内。
- 自动验收：六个独立 SVG 均无嵌入位图、filter、mask、clipPath 或冗余分组；浏览器验证 Dark / Light 外部资源加载、品牌 `36 × 36px`、五个 Tab `24 × 24px`、选中/未选中颜色继承与控制台无错误。
- 用户浏览器反馈后，顶部设置按钮同步使用 `tab-settings.svg` 造型，保持 `44 × 44px` 命中区和 `20 × 20px` 工具图标尺寸；Tab 选中底保持 `56 × 48px`，圆角调整为 `24px`，与外层 `64px` 高胶囊形成相差 `8px` 的同心弧线；导航边框改为不占布局空间的 `1px` 内描边，使首尾和上下视觉间距均精确为 `8px`。
- 输入状态目录中，Error 辅助文案曾撑高同一 Grid 行，并使相邻 Disabled 输入框因内部 Grid 拉伸而高于 `48px`；共享 `.kr-field` 改为顶部对齐内容，四种输入状态恢复一致的 `48px` 控件高度。

## VDA-03 执行记录

- 状态：已于 2026-07-13 通过自动验收并完成。
- 范围：建立 01 本地服务未连接、02 本地档案选择、03 创建电台档案的 Dark HTML 视觉页面与固定 fixture；04–15 保持 VDA-01 骨架，不生成 Light、响应式或正式基线截图。
- A / B 级差异：A-005、A-007 与 B-001 按 VDA-00 已确认方案执行；未发现新增或未关闭的 A / B 级差异。
- C 级裁决：C-001 统一使用主题 Surface、Border 与弱阴影；C-002 使用固定 `64 × 64px` 圆形低饱和抽象头像和 `88 × 88px` 圆形上传占位，不追随 PNG 随机图像裁切；C-003 将 01 的中断声波收敛为静态七柱状态图形；C-004 使用 4px 网格、统一卡片内边距，并在规范组件缩小后保留页面底部安全区，将 02 隐私说明及 03 隐私/操作组放入稳定底部区域；C-005 复用系统字体、品牌 SVG、Settings SVG 与线性工具图标。
- 结构实测：01 内容列 `x 200 / w 560px`、顶部工具区 `x 56 / y 56 / w 848 / h 48px`、Tab Bar `x 300 / y 1504 / 360 × 64px`；02/03 内容列 `x 120 / w 720px`、顶部工具区 `y 56 / h 48px`；02 三张档案卡均为 `152px` 高、`24px` 内边距、`64px 486px 72px` 三列；03 上传头像 `88 × 88px`、输入 `48px` 高、文本区 `116px` 高、操作组 `720 × 112px`。
- 自动验收：JavaScript 语法与 Git whitespace 检查通过；浏览器验证 01–03 文案和结构、15 个定位链接回归、04–15 骨架保持、Light 共享变量冒烟、画布均为 `960 × 1600px`、无画布溢出、无控制台错误，页面内所有按钮命中区均不小于 `44 × 44px`。
- 未验证范围：Light 视觉校准属于 VDA-10，响应式属于 VDA-12，完整交互与无障碍属于 VDA-13，正式 Dark / Light 基线截图与逐页视觉 QA 属于 VDA-14。

### 2026-07-13 Profile 卡片视觉重量校准

- 用户浏览器验收反馈 02 档案卡片的 `64 × 64px` 头像与右侧文字视觉重量不协调；继续保留 A-005 已确认的头像尺寸、三列结构和 `24px` 间距，不放大头像或改变内容层级。
- C 级裁决：档案名称从共享 H3 的 `18px / 600` 局部收敛为 `17px / 550 / 24px`，头像改用 Strong Border、轻微内描边并提高低饱和图形对比度，使头像和文字在相同几何下获得更均衡的感知尺寸。

## VDA-04 执行记录

- 状态：已于 2026-07-13 通过自动验收并完成。
- 范围：建立 04 Radio 空节目、05 Radio 正在播放、06 Radio 生成中的 Dark HTML 视觉页面与固定 fixture；三态复用同一 Radio 骨架，07–15 保持 VDA-01 骨架，不生成正式基线截图或产品行为。
- A / B 级差异：A-001、A-003 与 A-007 按 VDA-00 已确认方案执行；未发现新增或未关闭的 A / B 级差异。
- C 级裁决：C-001 统一使用主题 Surface、Border 与弱阴影；C-002 使用固定 `56 × 56px` 低饱和封面和 `36 × 36px` 档案头像；C-003 使用 20 柱共享波形，生成态使用中性灰并在 Reduce Motion 下停止动画；C-004 使用 4px 网格、`52px` 队列行和固定内部间距；C-005 复用系统字体、品牌 SVG、Tab SVG 与线性工具图标；C-006 保持底部导航固定几何。对话区将 DJ 文案呈现为自然段，仅用户输入使用弱气泡；生成态不展示绿色成功语义或虚构进度百分比。
- 结构实测：三态顶部工具区均为 `x 56 / y 56 / w 848 / h 48px`；时间 `x 200 / y 136 / 560 × 132px`；主内容 `x 200 / y 320 / 560 × 220px`；队列 `x 200 / y 592 / 560 × 256px`；DJ 状态 `x 200 / y 888 / 560 × 56px`；对话 `x 200 / y 976 / 560 × 284px`；输入 `x 200 / y 1400 / 560 × 72px`；Tab Bar `x 300 / y 1504 / 360 × 64px`。05 播放器为 `560 × 220px`，06 波形为 `400 × 56px`，播放与生成队列均为四行 `52px`。
- 自动验收：JavaScript 语法与 Git whitespace 检查通过；浏览器验证三态坐标零位移、状态/颜色语义、生成输入禁用、所有按钮与输入命中区不小于 `44 × 44px`、15 个定位链接回归、Light 共享变量冒烟、画布均为 `960 × 1600px`、无画布溢出且无控制台错误。
- 未验证范围：Light 独立视觉校准属于 VDA-10，Radio 响应式代表页属于 VDA-12，完整交互、键盘、200% zoom 与无障碍属于 VDA-13，正式 Dark / Light 基线截图与逐页视觉 QA 属于 VDA-14；VDA-04 与 VDA-05 的核心体验确认门将在 VDA-05 完成后统一验收。

## VDA-02–04 尺度补正记录

- 状态：2026-07-13 已完成 HTML、Tokens、组件目录与权威规范补正，并通过自动检查与用户视觉验收。
- 根因：早期 A-001、A-003、A-005、A-007、B-001–B-003 与 C-006 主动采用了远小于参考图的 rail、导航、头像、控件和字体规格；页面 CSS 又重复硬编码旧尺寸，导致 token 修改无法完整传递。定位器 Fit 未显示比例只增加了判断误差，不是相对比例错误的根因。
- 当前规格：Radio `x 72 / w 816px`；Profile `848px`；Management `840px`；Settings `832px`；顶部工具 `64px`；Tab Bar `620 × 88px`；全局输入与大按钮 `80px`，14 的 Settings 服务配置输入按 B-004 使用 `64px`；Radio 输入 `816 × 88px`；档案卡 `256px`；档案头像 `160px`；当前封面 `132px`；主播放按钮 `96px`。
- 页面范围：01 保留 `560px` 恢复内容列并放大文字、状态图形、操作与导航；02 使用 `848px` rail、`256px` 卡、`160px` 头像和统一 `32px` 间距；03 使用 `848px` rail、`144px` 上传头像和 `80px` 控件；04–06 共用新的七个 Radio 固定 slot，内部状态内容不同但共享边缘零位移。
- 防回归：原型定位器新增 `1:1 / Fit`、实时缩放百分比、参考 PNG 叠图开关与透明度控制；叠图只在 `prototype · 960 × 1600` 生效，其它 viewport 明确禁用。
- 浏览器反馈补正：05 当前播放器统一使用 `28px` 内边距，并以纵向等分布局保持卡片上、下留白一致以及进度条到上下内容区的间距一致；该值已进入共享 token、权威规范与提示词。
- 保持不变：fixture 文案、DOM 内容层级、SVG 文件与节点、图标造型、颜色、材质、圆角、阴影、动效风格和产品行为均未改变。
- 门禁：共享组件尺度确认点已关闭，VDA-05 可开始；完整核心体验门仍在 VDA-05 完成后与 Detail Sheet 07–08 一并确认。

## VDA-05 执行记录

- 状态：2026-07-13 已完成 HTML、CSS、固定 fixture、自动验收与视觉裁决记录；核心体验确认门通过。
- 范围：建立 07 Detail Sheet DJ 串讲与 08 Detail Sheet 歌词跟随的 Dark HTML 视觉页面；两态复用同一全屏渲染器、Detail Tokens 和固定骨架，只替换状态词、歌曲文案、歌曲进度与主内容卡文字。09–15 保持 VDA-01 骨架，不生成 Light、响应式或正式基线截图。
- A / B 级差异：A-002 按已确认方案执行固定全屏骨架；A-009 记录 PNG 双暂停入口与 PRD 单一入口的语义差异；A-010 记录第一轮浏览器视觉反馈；A-011 记录 Detail 专属内容列、卡片尺寸和纵向节奏的后续反馈，并在先更新权威规范后关闭。未发现新增 B 级差异或未关闭的 A / B 级差异。
- C 级裁决：C-001 统一使用 `#090A0C` 深色声场、`#FFFFFF` 节目面、`#F5F3F6` 内容卡与弱 Sheet 阴影；C-003 在 A-010 指定数量与交替色之外继续使用共享确定性柱高，节目进度使用 96 根确定性细柱；C-004 在 A-011 固定外部几何内保留 `40px` 卡片内边距，并使用 `space-evenly` 均匀分布串讲段落与歌词行，使串讲态上下留白由 `183px` 收敛到 `97px`、歌词态上下均为 `61px`；C-005 复用系统字体与线性关闭/暂停图标。所有新增尺寸均使用 `--kr-detail-*` 专属 Tokens，不改变其他页面族。
- 结构实测：07/08 均为 `x 0 / y 0 / 960 × 1600px`；拖动条 `x 438 / y 32 / 84 × 6px`；关闭按钮 `x 840 / y 40 / 64 × 64px`；状态区 `x 80 / y 76 / 720 × 40px`；64 柱交替绿白声波区 `x 0 / y 160 / 960 × 280px`；节目面 `x 0 / y 420 / 960 × 1180px`；标题 `x 56 / y 484 / 848 × 68px`、字号 `60px`；歌曲信息 `x 56 / y 584 / 848 × 32px`；歌曲进度 `x 56 / y 636 / 848 × 28px`；主内容卡 `x 56 / y 700 / 848 × 700px`；节目进度 `x 56 / y 1456 / 768 × 48px`；唯一播放/暂停按钮 `x 848 / y 1452 / 56 × 56px`，两者间距 `24px`。两态所有共享锚点的位置与尺寸差为 `0px`。
- 自动验收：JavaScript 语法、Git whitespace、文件格式和 PNG `960 × 1600px` 检查通过；浏览器验证两态的 Detail 专属内容列、标题、歌曲信息、歌曲进度、主卡、节目进度和播放按钮均与规范一致；64 根波形柱、32 根绿色柱逐根交替与单一播放控制保持不变，无禁用组件、无画布溢出，两态共享锚点差为 `0px`。既有 `dialog` / `aria-modal` / `aria-live` / progressbar 与 Reduce Motion 结构保持不变。标题、歌曲信息、当前行和未播放行对比度分别为 `18.11:1`、`4.78:1`、`16.42:1`、`3.91:1`。
- QA 证据：[07 DJ 串讲](evidence/vda-05-detail-speaking.png)、[08 歌词跟随](evidence/vda-05-detail-lyrics.png)。两张图是自动验收派生产物，不是 VDA-14 正式基线。
- 已知后续范围：规范指定的已读文本 `#A8A7A1` 在 `#F5F3F6` 上实测 `2.19:1`；VDA-05 保持明确视觉规范，不在页面族任务中静默改色，最终 WCAG 处理纳入 VDA-13。Light 独立校准属于 VDA-10，Detail 响应式代表页属于 VDA-12，正式 Dark / Light 基线与逐页视觉 QA 属于 VDA-14。

## VDA-06 执行记录

- 状态：2026-07-13 已完成 HTML、CSS、固定 fixture、自动检查与用户视觉验收，任务为“已完成”。
- 范围：建立 09 Library 音乐库 Dark HTML 页面；默认结果页包含搜索、五条歌曲、试听、加入候选池、导入网易云歌单、连接状态与已导入来源，并通过 `variant` 固定定位 `results`、`importing`、`empty`、`no-results`、`service-error` 五种视觉状态。10–15 保持 VDA-01 骨架，不生成 Light、响应式或正式基线截图。
- A / B 级差异：没有新增未决 A / B 级差异。页面结构、材质与单列密度沿用已确认 B-002；Management 内容列使用 2026-07-13 尺度校准后的 `840px`。用户在本任务方案确认中同意歌曲行以 `64px` 固定封面和 `16px` 内边距形成 `96px` 行高，不采用 `design/prompt.md` 中无法同时容纳该封面与统一内边距的旧 `72px` 描述。
- C 级裁决：C-001 使用共享 Surface、Border 与极弱阴影；C-002 使用五种固定 `64 × 64px` 低饱和抽象封面，不追随 PNG 随机裁切；C-004 使用 4px 网格、普通卡片 `20px` 圆角、导入卡 `24px` 内边距和统一分割线；C-005 复用系统字体、品牌 SVG、Settings SVG、Tab SVG 与线性试听、搜索、列表、重试图标。状态色同时配有 `CONNECTED`、`IMPORTING`、`OFFLINE` 或错误文案，不以颜色单独表达语义。
- 结构检查：画布目标保持 `960 × 1600px`；顶部工具 `x 60 / y 40 / w 840 / h 64px`；Management 主列 `x 60 / y 144 / w 840px`；搜索框 `840 × 80px`；歌曲封面 `64 × 64px`；五条结果各 `96px`；底部导航复用共享 `x 170 / y 1480 / 620 × 88px`。静态几何公式计算最长的 `importing` 变体内容底边为 `y 1380`，与导航顶边保留 `100px`。
- 自动检查：JavaScript 语法、fixture 冻结、15 页 / 3 主题 / 4 viewport 回归、五种 Library 变体、五首歌曲、两条来源、关键 CSS 几何、参考 PNG `971 × 1619px`、Git whitespace 与禁止目录范围均通过。Dark 主题主要文字、次要文字、Error、Success、Information 在对应背景上的对比度分别不低于 `18.12:1`、`8.51:1`、`5.60:1`、`7.61:1`、`5.91:1`。
- 视觉验收：浏览器安全策略拒绝 Agent 读取本地 `file://` 页的 DOM 或截图，因此未生成 VDA-06 QA 派生截图，也未取得真实 computed geometry、控制台与 PNG 叠图自动证据；用户在 Codex 应用内打开 Dark Library 页面并检查五种变体后明确确认“视觉通过”，关闭 VDA-06 视觉验收门。Light 校准属于 VDA-11，Management 响应式代表页属于 VDA-12，完整交互与无障碍属于 VDA-13，正式 Dark / Light 基线属于 VDA-14。

## VDA-07 执行记录

- 状态：2026-07-13 已完成 HTML、CSS、固定 fixture、自动浏览器视觉验收与裁决记录，任务为“已完成”。
- 范围：建立 10 Taste 品味概览与 11 Taste 编辑 Dark HTML 页面；概览通过 `variant` 固定定位 `formed`、`loading`、`empty`、`load-error`，编辑通过 `variant` 固定定位 `editing`、`saving`、`save-error`。12–15 保持 VDA-01 骨架，不生成 Light、响应式、Figma 或正式基线截图。
- A / B 级差异：没有新增或未关闭的 A / B 级差异。A-004 按已确认产品语义将 10 底部第三项 Taste 设为选中，不追随 PNG 错选的 Library；B-002 与 2026-07-13 尺度校准继续规定 `840px` Management 单列、克制 Surface 和单列信息层级。11 按任务方案确认不显示主导航，使用全宽半透明固定底部背景与内部 `x 60 / w 840px` 操作 rail。
- C 级裁决：C-001 统一使用共享 Surface、Border 与极弱阴影；C-003 将四项品味分布实现为确定性线性比例条，仅最高项 Calm 的进度端点使用状态绿，不采用雷达图、饼图或随机纹理；C-004 使用 4px 网格、普通卡片 `20px` 圆角和 `20 / 24px` 内边距，概览页在单列内并排“喜欢的声音 / 避雷规则”两张同级卡，编辑页严格保持 `80px` 规则行、`136px` 场景卡和固定操作区；用户浏览器反馈后，场景名称与偏好描述输入统一为 `52px` 高，偏好描述使用 `15px` 上下内边距使 `20px` 行高文字垂直居中，保持同级控件的纵向尺寸与文字对齐一致；C-005 复用系统字体、品牌 SVG、Settings SVG、Tab SVG 与统一线性编辑、反馈、场景、拖动和删除图标。卡片内 12–14px 小号时间、字段标签和字数提示使用 Secondary 而非 Third-level，避免 `#737B87` 在 `#111317` 上仅 `4.35:1`。
- 结构实测：10 顶部工具 `x 60 / y 40 / w 840 / h 64px`，主列 `x 60 / y 128 / w 840px`，概览卡 `x 60 / y 276 / 840 × 212px`，完整内容底边 `y 1315`，底部导航复用 `x 170 / y 1480 / 620 × 88px` 并选中 Taste；11 主列 `x 60 / y 128 / w 840px`，标签容器 `y 296 / h 152px`，规则容器 `y 520 / h 305px`，三张场景卡组成 `y 897 / h 432px`，内容底边 `y 1329`，固定操作背景 `x 0 / y 1456 / 960 × 144px`，内部 rail `x 60 / w 840px`，无底部主导航。两页均无画布溢出，内容与固定区域之间保留安全间距。
- 自动验收：JavaScript 语法、fixture 冻结、页面 / 主题 / viewport 定位、七种 Taste 变体、12–15 占位边界、关键 DOM 语义、Git whitespace 与禁止目录范围均通过；浏览器逐态验证期望文案、恢复入口、Taste 导航语义、编辑页无主导航、保存中禁用、保存失败保留内容、无画布溢出、无控制台错误，所有按钮命中区均不小于 `44 × 44px`。Dark 主题主要文字、次要文字、第三层文字在页面背景、错误文字在 Surface、状态绿在 Surface、主要按钮文字的对比度分别为 `18.12:1`、`9.06:1`、`4.63:1`、`5.60:1`、`7.61:1`、`16.88:1`；卡片内小号文字使用 Secondary 后为 `8.51:1`。
- 视觉验收：通过本地只读静态服务在 Codex 内置浏览器渲染 `960 × 1600px` Dark 画布，检查 10 概览与 11 编辑的实际排版、computed geometry、七种状态、控制台和焦点/命中区；临时 QA 地址不构成 Koradio 产品已确认端口。结果与已确认 PNG 的冷调表面、编辑排版和呼吸感一致，且按 A-004 修正导航语义，未发现需暂停的 A 级或需集中确认的 B 级差异。Light 校准属于 VDA-11，Management 响应式代表页属于 VDA-12，完整键盘、200% zoom、Reduce Motion 与无障碍属于 VDA-13，正式 Dark / Light 基线属于 VDA-14。

## VDA-08 执行记录

- 状态：2026-07-13 已完成 HTML、CSS、固定 fixture、自动浏览器视觉验收与裁决记录，任务为“已完成”。
- 范围：建立 12 Programs 节目历史列表与 13 Program 历史详情 Dark HTML 页面；列表通过 `variant` 固定定位 `list`、`loading`、`empty`、`load-error`，详情通过 `variant` 固定定位 `detail`、`replaying`、`tts-missing`、`reuse-error`。14–15 保持 VDA-01 骨架，不生成 Light、响应式、Figma 或正式基线截图。
- A / B 级差异：没有新增或未关闭的 A / B 级差异。A-006 按已确认方案将 12 的大前导封面降为三个 `44 × 44px` 低饱和辅助封面，节目卡严格使用 `168px` 高、`20px` 内边距的编辑式结构；B-002 与 2026-07-13 尺度校准继续规定 `840px` Management 单列、克制 Surface 和单列信息层级。
- C 级裁决：C-001 统一使用共享 Surface、Border 与极弱阴影；C-002 使用固定 `44 × 44px` 节目辅助封面和 `64 × 64px` 队列封面，不追随 PNG 随机裁切或提取主题色；C-004 使用 4px 网格、普通卡片 `20px` 圆角和统一 `16 / 20 / 24px` 内边距，本周摘要的七日刻度在固定基线上对齐；Program 队列沿用 Library 已确认的 `64px` 封面加上下各 `16px` 内边距，形成 `96px` 行高，解决提示词中 `64px` 行高无法同时容纳 `64px` 封面与内边距的内部矛盾；C-005 复用系统字体、品牌 SVG、Tab SVG 与统一线性搜索、返回、收藏、播放和更多图标。卡片内 11–14px 小号标签使用 Secondary，避免 Tertiary 在 Surface 上仅 `4.35:1`。
- 结构实测：12 顶部工具 `x 60 / y 40 / w 840 / h 64px`，主列 `x 60 / y 128 / w 840px`，本周摘要 `x 60 / y 292 / 840 × 196px`，四张节目卡均为 `840 × 168px` 并组成 `y 512 / h 720px` 列表；13 顶部工具 `x 60 / y 40 / w 840 / h 64px`，主列 `x 60 / y 128 / w 840px`，场景卡 `x 60 / y 316 / 840 × 208px`，开场区 `x 60 / y 556 / 840 × 152px`，队列 `x 60 / y 736 / w 840px`，四行均为 `96px`，反馈区底边 `y 1357`；两页底部导航均复用 `x 170 / y 1480 / 620 × 88px` 并选中 Programs。详情四种变体的场景、开场、队列和反馈锚点差均为 `0px`。
- 自动验收：JavaScript 语法、fixture 冻结、15 页定位、八种 Programs 变体、14–15 占位边界、关键 DOM 语义、Git whitespace 与禁止目录范围均通过；浏览器逐态验证期望文案、恢复入口、Programs 导航语义、重播进度、音频缺失降级、复用失败提示、无画布溢出、无控制台错误，所有按钮命中区均不小于 `44 × 44px`。Dark 主题主要文字、次要文字在背景和 Surface、Error、Warning、Information 与主要按钮文字的对比度分别为 `18.12:1`、`9.06:1`、`8.51:1`、`5.60:1`、`8.82:1`、`6.30:1`、`16.88:1`。
- 视觉验收：通过本地只读静态服务在 Codex 内置浏览器渲染 `960 × 1600px` Dark 固定画布，对照 12–13 PNG 检查节目档案气质、编辑排版、场景操作、自然段串讲、队列和反馈摘要；临时 QA 地址不构成 Koradio 产品已确认端口。结果按 A-006 保留 PNG 的日期、标题、场景与低饱和媒体观感，同时服从已确认的小封面编辑卡结构，未发现需暂停的 A 级或需集中确认的 B 级差异。Light 校准属于 VDA-11，Management 响应式代表页属于 VDA-12，完整键盘、200% zoom、Reduce Motion 与无障碍属于 VDA-13，正式 Dark / Light 基线属于 VDA-14。
- 用户浏览器反馈后，本周摘要的“节目 / 时长 / 歌曲”三栏使用等宽三分网格定位：第一组靠左、时长组位于卡片水平中心、歌曲组靠右；每组按自身最大内容宽度收缩，上下两行统一左对齐。卡片尺寸、内边距、文案层级和七日图表均保持不变。

## VDA-09 执行记录

- 状态：2026-07-14 已完成 HTML、CSS、固定 fixture、自动浏览器视觉验收与裁决记录，任务为“已完成”。
- 范围：建立 14 Settings 服务配置与 15 Settings 服务检测 Dark HTML 页面；配置页通过 `variant` 固定定位 `configured`、`detecting`、`incomplete`、`save-error`，检测页通过 `variant` 固定定位 `degraded`、`available`、`core-error`。不生成 Light、响应式、Figma 或正式基线截图。
- A / B 级差异：没有未关闭的 A / B 级差异。A-008 按已确认产品语义使用 `Theme Mode`，不追随 PNG 的 `Theme`；B-004 按 2026-07-14 用户浏览器反馈将 14 的服务配置输入框从 `80px` 校准为 Settings 专属 `64px`，主要区块间距统一为 `24px`，并移除 DJ Voice Style 选择框下的重复说明。`832px` Settings 单列、克制 Surface、单列信息层级、15 检测页结构及产品行为保持不变。
- C 级裁决：C-001 统一使用共享 Surface、Border 与极弱阴影，不追随 PNG 局部颗粒；C-004 使用 4px 网格、`60px` 服务状态行、`20px` 检测卡圆角、`24px` 检测卡内边距和固定底部操作区；用户反馈后，偏好设置三项控件统一为最小命中区 `44px` 高并以两个网格单位形成 `8px` 行间距，本地数据标题到卡片的内部间距收敛为 `8px`，不改变主要区块的 `24px` 节奏；Test 与 Change 继续保留 `44px` 命中区，但使用同步的 `32px` 内收可见边框和上下各 `6px` 安全间距，避免边框与外层卡片重叠；C-005 复用系统字体、品牌 SVG、Settings Tab SVG 与统一线性返回、显示密钥、下拉、检测和警告图标；C-006 复用 `620 × 88px` 共享导航。密钥 fixture 只保留后四位 `7K2M`，配置缺失使用 Warning 文字与状态点而不沿用成功色；检测中使用 Information 状态并在 Reduce Motion 下停止脉冲。状态均同时提供文字，不以颜色单独表达。
- 结构实测：14 顶部工具 `x 64 / y 40 / w 832 / h 64px`，主列 `x 64 / y 128 / w 832px`，四个配置输入均为 `832 × 64px`；偏好设置三个控件均为 `552 × 44px`，相邻控件间距均为 `8px`；Test 与 Change 命中区均高 `44px`，可见边框均高 `32px` 并保留上下各 `6px` 净距；服务状态、服务配置、偏好设置、本地数据、操作区和导航依次从 `y 216 / 524 / 980 / 1196 / 1392 / 1480` 开始，相邻区块边界均保留 `24px`，固定操作区为 `832 × 64px`；15 主列 `x 64 / y 144 / w 832px`，四张检测卡中普通卡高 `156px`、展开卡高 `388px`，操作区从 `y 1280` 开始。两页底部导航均复用 `x 170 / y 1480 / 620 × 88px` 并选中 Settings；七种变体均无画布溢出。
- 自动验收：JavaScript 语法、fixture 冻结、15 页 / 3 主题 / 4 viewport 共 420 条唯一定位组合、七种 Settings 变体、关键 DOM 语义、15 页 Dark 默认状态回归、Git whitespace 与禁止目录范围均通过；本机只读 headless 浏览器逐态验证四个服务输入均为 `832 × 64px`、三个偏好控件均为 `552 × 44px`、两处偏好行间距均为 `8px`、五处主要区块间距均为 `24px`，Test / Testing 与 Change 均保持 `44px` 命中区、`32px` 可见边框和 `6px` 上下净距，同时确认必需文案、恢复入口、密钥遮蔽、Settings 导航语义、无画布溢出、无控制台错误，所有可用按钮和输入命中区均不小于 `44 × 44px`。Dark 主题主要文字、次要文字在背景和 Surface、Success、Warning、Error、Information 与主要按钮文字的对比度分别为 `18.12:1`、`9.06:1`、`8.51:1`、`7.61:1`、`8.28:1`、`5.60:1`、`5.91:1`、`16.88:1`。
- 视觉验收：通过临时只读静态服务渲染 `960 × 1600px` Dark 固定画布，对照 14–15 PNG 检查本地设置工具气质、服务列表、紧凑操作按钮、密钥层级、偏好控件边界、检测卡片、TTS 展开建议、局部降级提示和固定操作区；临时 QA 地址不构成 Koradio 产品已确认端口。结果保持 PNG 的冷调材质和信息密度，Test 与 Change 可见边框均已内收并与外层边框清晰分离，偏好控件边框已通过 `8px` 间距清晰分离，同时服从已确认的 `832px` rail、Settings 专属 `64px` 服务配置输入、`24px` 区块节奏、`Theme Mode` 与共享组件几何；B-004 已由用户反馈确认并关闭，未发现需暂停的 A 级或仍待确认的 B 级差异。Light 校准属于 VDA-11，Settings 响应式代表页属于 VDA-12，完整键盘、200% zoom 与无障碍属于 VDA-13，正式 Dark / Light 基线属于 VDA-14。

## VDA-10 执行记录

- 状态：2026-07-14 已完成 HTML / CSS Light 独立材质校准、自动浏览器视觉验收与裁决记录，任务为“已完成”。
- 范围：只校准 01 本地服务未连接、02 档案选择、03 档案创建、04–06 Radio 三态与 07–08 Detail Sheet 的 Light 渲染；不修改 fixture、产品行为、DOM 内容层级、响应式规则、09–15 Light、Figma 或正式基线。
- A / B 级差异：未发现新增或未关闭的 A / B 级差异。原始 PNG 只有 Dark 状态，Light 配色服从 `design/design.md`；PNG 继续作为结构、密度、组件比例与未被规范覆盖的观感参考。
- C 级裁决：新增 C-007，将 Light 核心页环境光改为 Light Accent 低透明度变量，并将 Radio 卡片与对话表面的弱渐变改为主题表面辉光变量；Dark 使用原有取值，07–08 固定使用 Detail 专属黑色声场、白色节目面与浅灰内容卡。随机波形、纸面颗粒和字体渲染继续按 C-003 / C-005 处理。
- 自动验收：本机 Chrome 150 通过临时只读静态服务逐页渲染 Light 01–08；八页均为 `960 × 1600px`，参考图资源全部加载，控制台与资源错误为 0，画布无溢出，可用控件最小命中区为 `48px`。Reduce Motion 条件下逐页对比 Dark / Light 的可见节点数量、顺序与几何，最大差为 `0px`。Light 主要 / 次要文字对页面背景为 `15.99:1` / `5.27:1`，主要按钮文字为 `17.24:1`；Detail 标题 / 白色节目面、次要文字 / 白色节目面、当前正文 / 内容卡分别为 `18.11:1`、`4.78:1`、`16.42:1`。
- QA 证据：[Light 汇总](evidence/vda-10-light-contact.png)、[原始 PNG / Light 并排对照](evidence/vda-10-light-comparison.png) 与八张 `vda-10-light-01–08` 派生截图；它们不是 VDA-14 正式截图基线。
- 已知后续范围：Detail 已读文本 `#A8A7A1` 在 `#F5F3F6` 上仍为 `2.19:1`，沿用 VDA-05 已记录的明确规范并留给 VDA-13 完整无障碍裁决。内置浏览器因运行时全局注入冲突未能建立连接，已按用户确认使用 Chrome 备用链路；页面验收本身已完成。09–15 Light 属于 VDA-11，响应式属于 VDA-12，正式基线属于 VDA-14。

## VDA-11 执行记录

- 状态：2026-07-14 已完成 HTML / CSS Light 独立材质校准、自动浏览器视觉验收与裁决记录，任务为“已完成”。
- 范围：只校准 09 Library、10–11 Taste、12–13 Programs 与 14–15 Settings 的 Light 渲染，以及这些页面的 27 个固定视觉变体；不修改 fixture、产品行为、DOM 内容层级、响应式规则、Figma 或正式基线。
- A / B 级差异：未发现新增或未关闭的 A / B 级差异。原始 PNG 只有 Dark 状态，Light 配色服从 `design/design.md`；PNG 继续作为结构、密度、组件比例与未被规范覆盖的观感参考。
- C 级裁决：新增 C-008，将 09–15 Light 环境光统一到主题变量，并按既有 C-001 原则把成功 / 警告色限制在状态点、图标和边框，小号状态文字使用 Secondary，错误文字使用 Error 与 Text Primary 的主题混合色加深；Dark 视觉和两主题几何保持不变。
- 自动验收：本机 Chrome 150 通过临时只读静态服务逐态渲染 09–15 Light 的 27 个固定变体；画布均为 `960 × 1600px`，7 张参考图全部以 `971 × 1619px` 成功加载，控制台、运行时与资源错误为 0，画布溢出为 `0px`，可用控件最小命中区为 `44px`。Reduce Motion 条件下逐态对比 Dark / Light 的可见节点数量、顺序与几何，最大差为 `0px`。
- 对比度：Light 主要 / 次要文字对页面背景为 `15.99:1` / `5.27:1`，主要 / 次要文字对 Surface 为 `17.60:1` / `5.80:1`，主要按钮文字为 `17.24:1`；加深错误文字对页面背景和 Surface 分别为 `5.21:1` / `5.73:1`。Accent、Warning 与 Information 状态点 / Focus 图形对 Surface 分别为 `3.44:1`、`3.91:1`、`4.29:1`，满足非文本对比要求。
- QA 证据：[Light 汇总](evidence/vda-11-light-contact.png)、[原始 PNG / Light 并排对照](evidence/vda-11-light-comparison.png) 与七张 `vda-11-light-09–15` 派生截图；它们不是 VDA-14 正式截图基线。
- 已知后续范围：跨 viewport 响应式属于 VDA-12，完整键盘、200% zoom、Reduce Motion 行为和无障碍属于 VDA-13，正式 Dark / Light 基线属于 VDA-14。

## VDA-12 执行记录

- 状态：2026-07-14 已完成 HTML / CSS / JavaScript 响应式代表页、自动浏览器视觉与结构验收，任务为“已完成”。
- 范围：使用 03、05、08、10、14 分别代表 Profile、Radio、Detail、Management、Settings，在 `390 × 844px`、`834 × 1194px`、`1440 × 1200px` 三组 viewport 下建立响应式布局；Dark / Light 使用同一 DOM 与几何。未复制页面 fixture，未修改产品源码、生产配置或 VDA-14 正式基线目录。
- A / B 级差异：未发现新增或未关闭的 A / B 级差异。响应式保持已确认页面骨架、内容顺序、主组件结构、主题气质和交互语义；Radio 宽屏仍为中央单列，Detail 始终全画布覆盖，管理与 Settings 均未引入侧栏。
- C 级裁决：新增 C-009，以共享 viewport tokens 统一三组安全边距、顶部间距、导航底部 safe area 与内容滚动边界；Mobile 共享顶部工具、导航、头像、封面和播放器控件按比例收敛但命中区不小于 `44px`，H1 与大号时间明确使用 `28px / 48px`；Radio 新增单一 `.radio-scroll` 供 04–06 继承，避免为 05 复制页面结构；Detail 继续保留 64 柱声波、完整歌词、节目进度与唯一播放控制。
- 结构实测：Mobile 五类 shell 分别为 `350 / 350 / 390 / 350 / 350px`，Tablet 为 `778 / 778 / 834 / 778 / 778px`，Desktop 为 `848 / 816 / 1440 / 840 / 832px`；Profile、Radio、Management、Settings 内容层的 `scrollHeight` 均大于 `clientHeight`，滚动至底部后末项完整可达，顶部与底部固定区域无位移。15 个 Dark 场景均无横向溢出。
- 自动验收：本机 Chrome 150 通过临时只读静态服务验证 5 页 × 3 viewport × 2 主题共 30 个场景；画布尺寸、shell 宽度、长文案、滚动边界、固定区域、安全区、横向溢出、可用操作 `44 × 44px` 最小命中区、Dark / Light 几何零位移、控制台与运行时错误均通过。另验证 5 个 Prototype 代表页原有固定锚点零回归，15 张 QA 派生截图尺寸均与对应 viewport 一致。
- QA 证据：[Mobile Radio](evidence/vda-12-mobile-05-radio-playing.png)、[Tablet Detail](evidence/vda-12-tablet-08-radio-detail-lyrics.png)、[Desktop Taste](evidence/vda-12-desktop-10-taste-overview.png)；全部 15 张 `vda-12-{viewport}-{page}.png` 只作为 VDA-12 自动验收证据，不是 VDA-14 正式基线。
- 已知后续范围：完整 hover / active / focus / disabled / loading / error、键盘路径、200% zoom、`aria-live`、Reduce Motion 与 Detail 已读歌词对比度属于 VDA-13；正式 Dark / Light 与响应式基线属于 VDA-14。

## VDA-13 执行记录

- 状态：2026-07-14 已完成 HTML / CSS / JavaScript 交互与无障碍资产、自动浏览器验收与裁决记录，任务为“已完成”。
- 范围：只补齐组件目录和 01–15 HTML 视觉主源的 Hover、Active、Focus、Disabled、Loading、Error、键盘路径、页面切换、Detail 开合、播放状态、live region、200% zoom 与 Reduce Motion；不修改 fixture、产品源码、生产配置、Figma 或 VDA-14 正式基线。
- A / B 级差异：A-012 已按用户确认将 Detail 已读正文从 `#A8A7A1` 调整为 `#898A86`，在 `#F5F3F6` 内容卡上由 `2.19:1` 提升到 `3.15:1`；未发现新增或未关闭的 B 级差异。
- C 级裁决：新增 C-010，使用共享时长完成页面淡入、Detail 位移开合、主导航方向键、Modal 焦点循环与关闭回收；新增 C-011，统一控件交互反馈、Loading 静态降级、错误字段关联、切换状态语义以及暂停后的文字 / 播报 / 波形同步。未改变布局、组件层级、内容层级或产品语义。
- 自动验收：JavaScript 语法与 Git whitespace 检查通过；本机 Chrome 150 经临时只读静态服务验证 15 页共 185 个可见可用控件，命中区均不小于 `44 × 44px`；方向键、Home / End、页面 H1 聚焦、Library `⌘/Ctrl+K`、Programs 场景复用到 Radio 待发送态及发送后进入生成态、Detail Tab / Shift+Tab 循环、Escape 关闭与焦点回收均通过。9 个 Loading / Error 代表状态具备对应 `role`、`aria-live` 或 `aria-busy`；暂停同步 `aria-pressed`、`PAUSED`、live region 与静止波形；浏览器无障碍树包含 heading、button、navigation、textbox 和 status 结构。200% 页面缩放下内容可滚动且导航保持可用；Reduce Motion 下页面、Detail、波形、骨架和状态脉冲停止。
- QA 证据：[键盘 Focus](evidence/vda-13-keyboard-focus.png)、[Reduce Motion Detail](evidence/vda-13-reduced-motion.png)、[200% Zoom](evidence/vda-13-zoom-200.png)。三张图是 VDA-13 自动验收派生产物，不是 VDA-14 正式基线。
- 已知后续范围：Dark / Light 各 15 张 `960 × 1600px` 正式基线、响应式正式基线和逐页视觉 QA 属于 VDA-14；Figma 派生镜像与开发交接映射分别属于 VDA-15、VDA-16。
- 浏览器反馈补正：2026-07-14 用户反馈 Detail 在 Fit 预览中开合过快、近似闪现；C-010 已改为 `560ms / 440ms`、`160px` 专属开合节奏，关闭由 `animationend` 驱动后再恢复 Radio。Chrome 150 实测进入 / 退出关键帧位移与透明度单调连续，关闭完成约 `540ms`（含页面切换），焦点返回 DJ 状态；Reduce Motion 下 `35ms` 内直接关闭且焦点保持正确。VDA-14 的 60 张正式基线复渲染全部通过，最大像素差异率仍为 `0.55%`。

## VDA-14 执行记录

- 状态：2026-07-14 已完成正式截图基线、自动视觉 / 结构 / 文件 QA、人工联系表复核与独立复渲染验证，任务为“已完成”。
- 范围：从现有 HTML / CSS / JavaScript 主源派生 01–15 Dark 与 Light 各 15 张 `960 × 1600px` Prototype 基线，以及 03、05、08、10、14 在 Mobile `390 × 844px`、Tablet `834 × 1194px`、Desktop `1440 × 1200px` 下的 Dark / Light 基线 30 张，合计 60 张；同时建立清单、可复现生成 / 验证脚本与四张 QA 联系表。不修改产品源码、包管理器、生产配置、布局、fixture、交互语义或 Figma。
- A / B 级差异：A-001–A-012 与 B-001–B-004 均保持已确认关闭状态；本次逐页、跨主题和跨 viewport 复核未发现新增 A / B 级差异。
- C 级裁决：新增 C-012，固定设备像素比、Reduce Motion、参考叠层、资源与布局等待条件，并以文件 SHA-256 验证正式产物完整性；复渲染比较按既有字体抗锯齿容差使用解码像素差异率，不静默放宽布局、内容或几何标准。
- 自动验收：60 张 PNG 均存在且尺寸与目标 viewport 一致；必需文字、画布裁切、固定比例、可用操作 `44 × 44px` 最小命中区、01 / 15 恢复入口、资源与运行时错误、Radio 04–06 与 Detail 07–08 跨状态几何、Dark / Light 几何零位移均通过。Dark 主要 / 次要文字和主要按钮对比度为 `18.12:1` / `9.06:1` / `16.88:1`，Light 为 `15.99:1` / `5.27:1` / `17.24:1`，Detail 已读大字为 `3.15:1`。最新独立复渲染 60 张全部通过，最大解码像素差异率为 `0.55%`，低于 `0.6%` 字体抗锯齿容差。
- 视觉验收：Dark / Light Prototype 联系表与两主题响应式联系表均已复核，并单独检查 07–08 Detail 及其代表性 Mobile / Desktop 渲染；结果保持已确认 PNG 的气质及既有规范几何，未见裁切、内容缺失、主题错位或响应式破坏。
- 交付物：`design/assets/baselines/`、`design/assets/baselines/manifest.json`、`design/assets/scripts/vda-14-baselines.cjs` 与 `design/assets/reports/evidence/vda-14-*.png`。
- 已知后续范围：Figma 派生镜像属于 VDA-15，页面—状态—组件—token—fixture—基线开发交接与整体冻结门属于 VDA-16；VDA-15 完成前不得把整体视觉冻结门描述为已通过。
- 浏览器反馈补正：2026-07-14 按 C-013 两次修正 11 Taste 编辑页标签内部水平居中与右侧关闭 SVG 的 `1px` 光学对齐；Dark / Light 两张正式基线与清单已重新生成，其余 58 张基线无文件差异；VDA-14 完成状态不变。

## VDA-15 执行记录

- 状态：2026-07-14 已完成 Figma 派生镜像、文件内转换限制说明、结构审计与人工视觉复核，任务为“已完成”。
- 文件：[Koradio · Visual Baseline · MVP](https://www.figma.com/design/ZxAWTQW5aH3VMd9H3T8zcJ)；生成所依据的 HTML Git 版本为 `325b604`，同步日期为 `2026-07-14`。
- 范围：建立 Cover、Getting Started、Foundations、Components、Screens · Prototype、Screens · Responsive、Utilities & Limits 与 3 个分隔页，共 10 页；导入 30 张 Prototype 与 30 张 Responsive 已验收基线 Frame。
- 原生资产：建立 6 个变量集合、209 个变量、64 个别名、10 个文字样式、4 个效果样式，以及 Button、Input、Status、Media、BottomNavigation、Card、DJStatus 7 个组件集共 66 个变体。
- A / B 级差异：未发现新增 A / B 级视觉差异；页面 Frame 与 VDA-14 正式基线保持一致。
- C 级裁决：新增 C-014；以已验收 PNG 图像填充保证页面保真，以原生变量、样式和代表性共享组件承载可编辑设计规则，并记录 SF Pro、响应式计算和交互转换限制。未修改 HTML / CSS / JavaScript、正式基线、布局、内容层级或产品语义。
- 自动验收：10 页顺序正确；209 个变量无断链别名、无 `ALL_SCOPES`、均有 Web code syntax；10 个文字样式、4 个效果样式存在；7 个组件集共 66 个变体，无未命名变体与重叠；Prototype 为 30 张且 Dark / Light 各 15 张，Responsive 为 30 张且 Mobile / Tablet / Desktop 各 10 张。Cover、Getting Started、Foundations、Components、Utilities & Limits 及两张页面联系表已人工复核，未见裁切、内容缺失或主题错位。
- 已知后续范围：页面—状态—组件—token—fixture—基线的开发交接映射与整体视觉冻结门属于 VDA-16；VDA-15 完成不代表整体视觉冻结门已经通过。

## VDA-16 执行记录

- 状态：2026-07-14 已完成开发交接映射、前端视觉验收清单、冻结版本记录与自动验收，任务为“已完成”。
- 冻结标识：`Koradio Visual Freeze · MVP · VDA-16 · 2026-07-14`；HTML / CSS / JavaScript 主源版本为 `325b604c5749594ebc89dd32526d52fd205a6936`，正式基线清单版本为 `5ffa59749d392faafd0d2a296e2579e56086f44c`。
- 交付物：`design/assets/reports/handoff-map.md`，建立 15 页、35 个固定状态、12 个组件组、13 个 token 组、fixture / renderer、60 张正式基线和五类响应式继承规则的追溯关系，并形成前端实现视觉验收清单与冻结后变更流程。
- A / B 级差异：未发现新增 A / B 级差异；A-001–A-012 与 B-001–B-004 保持已关闭状态。
- C 级裁决：新增 C-015，显式记录 15 个默认页面正式基线、20 个非默认 HTML 变体、`pages.js` 主要固定内容与 `app.js` renderer-local 状态文案的覆盖边界；不修改已验收 HTML、PNG 或 Figma。
- 自动验收：15 页、35 个状态、12 个组件组、13 个 token 组和 60 张正式基线映射通过；60 张基线 SHA-256 完整性通过；VDA-14 独立复渲染全部通过，最大像素差异率 `0.55%`；JavaScript 语法、JSON、Markdown 链接、Git whitespace 与禁止目录范围检查通过。
- 门禁：VDA-14、VDA-15 依赖满足，整体视觉冻结门通过。后续视觉变化必须先更新相应权威文档和 HTML 主源，再重新生成正式基线与 Figma 派生物。

## VDA-17 执行记录

- 状态：2026-07-15 已完成用户视觉反馈校准、验收、派生资产刷新与重新冻结，任务为“已完成”。
- 冻结标识：`Koradio Visual Freeze · MVP · VDA-17 · 2026-07-15`；HTML / CSS / JavaScript、权威设计文档与正式基线版本为 `6e97fb74826cdd48e5f75fe57646ac55340aab3c`。
- 范围：更新 Radio 04–06 的时间字体、共享垂直 slot、播放器密度与 DJ 对话高度；更新 Detail 07–08 的串讲词密度、顶部声波几何 / 配色 / 传播动效与节目进度形态；不修改产品行为、页面层级、fixture 内容、交互入口或其它页面族规格。
- A / B 级差异：未发现新增 A / B 级未决差异；A-001–A-012 与 B-001–B-004 保持已关闭状态。
- C 级裁决：新增 C-016，记录用户确认的冻结后视觉校准及主源、基线、Figma 同步范围。
- 自动验收：JavaScript 语法、Git whitespace、60 张正式基线生成与独立复渲染均通过；最大像素差异率 `0.4%`，低于 `0.6%` 容差；结构检查覆盖必需文字、画布 / 裁切、固定比例、`44 × 44px` 命中区、恢复入口、运行时错误、Radio / Detail 几何、Dark / Light 一致性和代表性 WCAG 对比度。动态检查确认波峰按同一方向传播、暂停保持静止、Reduce Motion 关闭动画。
- Figma：现有文件的 Prototype 30 帧与 Responsive 30 帧全部按新基线刷新；60 个图片填充哈希全部匹配，Cover 页面无临时节点残留，并完成桌面 Radio、Detail 串讲与移动端 Radio 抽检。
- 门禁：整体视觉冻结门以 VDA-17 版本重新通过；后续视觉变化继续遵守“权威文档 → HTML 主源 → 正式基线 → Figma 派生镜像 → 冻结记录”的顺序。
