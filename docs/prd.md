# Koradio 产品需求文档

## 一、概述

**文档信息**

| 版本号 | 日期 | 作者 |
|--------|------|------|
| V 1.1.0（开发前校准） | 2026-07-15 | 用户 / Codex |

### 1. 需求背景

- **目标用户现状**：目标用户是有长期听歌习惯、收藏歌单和明确音乐品味的音乐重度用户。他们会根据工作、写作、通勤、夜晚放松等场景主动找歌，但现有音乐产品多以算法流、榜单、歌单广场为主，难以像私人 DJ 一样理解当下语境并完成策展式播放。
- **核心痛点**：用户想要“说一句场景需求，就得到一段有开场、有串讲、有歌单、有播放反馈的私人 AI 电台”，而不是自己反复搜索、切歌、拼歌单。
- **当前问题汇总**：

| 问题点 | 问题描述 | 当前影响 |
|--------|----------|----------|
| 场景理解弱 | 普通播放器只知道歌曲和歌单，不理解用户此刻在做什么、想要什么氛围 | 用户需要手动搜索、试错和切歌 |
| 推荐解释弱 | 算法推荐通常不给出有品味、有情境感的推荐理由 | 用户感觉像被动接收列表，而不是被私人 DJ 策展 |
| 电台体验断裂 | 播放器、歌词、AI 文案、语音串讲通常分散在不同产品或功能里 | 无法形成“开场-播放-转场-反馈”的完整电台闭环 |
| 个人品味沉淀弱 | 用户的歌单、反馈、场景偏好没有被整理成可读、可编辑的个人品味档案 | 产品难以越用越懂用户 |

### 2. 需求目标

- **业务目标**：
  - 以“场景输入到电台播放”为 MVP 核心体验；`2–3 周` 仅是脚手架确定后的非承诺性估算，不作为发布日期或验收条件。
  - 支持本地多档案使用，让不同内测用户能在同一台机器上拥有独立品味和历史；设备级服务配置不随档案复制。
  - 建立可扩展的本地服务架构，为后续 24/7 自动电台、日程联动和更多音乐源预留空间。

- **成功标准**：

| 指标 | 当前状态 | 目标状态 |
|------|----------|----------|
| 场景点歌闭环 | 当前无产品实现 | 在 Codex 与网易云可用时，提交合法场景后生成至少 1 首可播放歌曲和 1 段开场文字；新节目提交成功后原子切换并开始播放 |
| 首次启动 | 当前无配置流程 | 首次启动自动选择可写的 OS 应用数据目录；用户只需创建档案并完成 Codex 配置，内置网易云 Provider 通过健康检查后即可发起首次生成 |
| DJ 串讲降级 | 当前无串讲 | TTS 缺失、无效或超时时仍保存文字 DJ，时间线不创建伪音频项，歌曲可继续播放 |
| 推荐反馈沉淀 | 当前无反馈记录 | 喜欢、取消喜欢、不喜欢、取消不喜欢、节目收藏、取消收藏和跳过均追加显式事件，重建自动品味时不覆盖人工规则 |
| 异常可恢复性 | 当前无错误处理 | 完全离线、生成失败、Profile 切换、数据迁移失败和多标签接管均有确定结果，不破坏既有节目、旧数据或人工设置 |
| 视觉基线一致性 | VDA-17 已冻结 | VDA-17 HTML 主源与 60 张 `960 × 1600px` 正式基线持续通过复渲染校验；语义修正不产生像素差异 |

### 3. 需求范围

- **包含**：
  - 本地 Web/PWA 竖版电台界面。
  - Node.js 本地服务中枢。
  - 本地 Codex 编排：读取用户档案、生成节目计划、输出结构化播放指令。
  - 本地服务内置的网易云 Provider：通过 TypeScript `linuxapi` 适配器完成音乐搜索、歌曲信息、播放链接和歌词获取，v1 不要求用户填写网易云地址或密钥。
  - 可选 Qwen3-TTS：由 bundled Python/MLX helper 调用固定 revision 的 8-bit 本地模型；中文固定 `Serena`、英文固定 `Ryan`，模型由用户首次下载，不可用时降级为完整文字 DJ。
  - 本地多档案：用户档案、品味标签、播放历史和反馈；Codex 与数据目录属于设备级配置，网易云与 TTS 属于设备级运行时能力。
  - Radio 主播放页：顶部品牌区、时间/ON AIR、播放器、队列、DJ 状态栏、DJ 对话区、底部输入的单列固定顺序。
  - DJ 状态栏详情态：全屏沉浸节目界面、深色声波区、浅色节目面、AI 串讲词、歌词、歌曲进度和节目整体进度。
  - 昼夜模式：夜间和日间都按冷调高级电台风格完整设计。
  - AI DJ 人格与语言：英式电台主持人气质，支持中文或英文串讲。
  - Library、Taste、Programs/History、Settings 辅助页面。

- **不包含**：
  - 云同步、跨设备同步和云端账号系统。
  - 商业支付、订阅、会员权益。
  - 公开社区、歌单广场、多人同时收听。
  - 完整 24/7 自动电台和复杂日程自动编排。
  - 多音乐源聚合，第一版仅以网易云 API 为 MVP 音乐源。
  - 真实音频波形预分析，MVP 可使用前端模拟动态柱状图。

### 4. 参考资料

| 文档名称 | 链接/说明 |
|----------|-----------|
| 竞品参考 | `design/references/source/AI音乐电台结构图.jpg`、`design/references/source/AI音乐电台施工图.jpg` |
| 原型参考 | `design/references/` 中的 15 张高保真 PNG 为视觉参考；`design/assets/prototype/` 已成为可执行 HTML 设计主源，当前 01–15 均已建立完整 Dark 页面 |

### 4.1 运行模式与数据来源

- Production Server、正式 PWA 和 macOS launcher 默认使用 `live`；Development、Test、CI 和显式 Demo 使用 `mock`。显式环境变量配置始终优先。
- Radio、Library 和 Settings 必须显示 `LIVE` 或 `DEMO MODE`，不得用 `CONNECTED` 暗示当前连接了真实音乐服务。
- Program、MusicTrack 和 PlaylistSource 保存 `originMode = live | mock`。既有演示数据只按稳定 Mock source ID 迁移标记，不按标题、歌手或场景文本模糊判断。
- Live 模式的候选池数量和策展输入排除 Demo 数据；历史 Demo 节目保留并显示 `DEMO` 标识，用户可自行永久删除。
- Mock Provider 必须返回同源、可播放的确定性媒体 fixture，不得返回无效占位域名。
| 设计规范 | `design/design.md`，用于约束视觉系统、组件细节、原型画布和页面状态 |
| 视觉开发基线 | `design/assets/prototype/` 为 VDA-17 HTML 主源，`design/assets/baselines/` 的 60 张图片为正式像素基线，`design/assets/reports/handoff-map.md` 为开发交接索引 |
| 历史视觉任务 | `design/tasks/visual-assets.md`、根目录 `design-qa.md` 仅保留过程与历史结论，不属于开发必读入口 |

### 5. 产品体验与视觉原则

**体验气质**

Koradio 的第一版体验应是“私人电台陪伴感 + 简约高级风格”。产品需要像一个克制、懂音乐、不会过度打扰的私人 DJ，而不是工具型音乐控制台。界面保留点阵字体、`ON AIR`、状态灯、波形、队列、`LIVE` 状态等电台识别元素，但避免复杂装饰、过度拟物和大面积花哨渐变。

UI 品牌字标使用全大写 `KORADIO`，字距略微拉开；正文叙述和文档中仍使用产品名 Koradio。Logo 应采用单色几何低细节设计，避免直接使用麦克风、耳机、唱片等常见音乐应用符号。

**昼夜模式**

| 模式 | 视觉基准 | 使用规则 |
|------|----------|----------|
| Night | 深黑、墨蓝、低亮度边框、绿色状态光、白色点阵数字 | 默认首选模式，突出深夜私人电台感 |
| Day | 冷白、浅灰、黑色正文、绿色状态光、低饱和分割线 | 与 Night 使用同一布局和组件结构，不做简单反色 |

用户可在 Settings 中为当前 Profile 选择 `Dark`、`Light` 或 `System`。MVP 不根据歌曲封面自动变色；当前歌曲可影响小范围状态光或波形强弱，但不得改变整体主题基准。

**语言规则**

Radio 主播放页和 Radio Detail Sheet 尽量使用英文状态词，例如 `ON AIR`、`LIVE`、`QUEUE`、`PLAYING`、`SPEAKING`、`THINKING`、`Now Playing`、`Say something to the DJ...`。Onboarding/Profile、Library、Taste、Programs/History、Settings 以中文为主，降低配置和管理成本。DJ 串讲语言由当前 Profile 的 `DJ Language` 决定，支持中文或英文，不做全应用完整国际化。

**AI DJ 人格与文案规则**

| 维度 | 规则 |
|------|------|
| 人格 | 英式电台主持人气质，声音柔和有磁性，专业克制，带轻微冷幽默 |
| 说话时机 | 每次节目必须有开场；只在情绪切换、风格变化、用户追问或关键歌曲前后做转场 |
| 说话长度 | 开场 20-45 秒；转场 8-20 秒；结束语 10-25 秒 |
| 内容范围 | 可简单介绍歌曲背景、风格特点、年代、场景关联或推荐理由 |
| 禁止内容 | 不油腻、不煽情过度、不装腔、不长篇百科、不每首歌都打断、不编造无法确认的音乐事实 |
| 失败降级 | 无法确认歌曲背景时，只描述可感知的氛围和用户场景，不输出虚构资料 |

**设计落地约束**

| 维度 | 规则 |
|------|------|
| 原型画布 | 高保真原型以 `960 × 1600px`、`3:5` 竖版桌面应用截图为基准，不展示设备模型、浏览器外框或营销落地页 |
| 内容列宽 | Radio `816px`；服务异常恢复区 `560px`；Profile/Onboarding `848px`；Library/Taste/Programs `840px`；Settings `832px`；Detail Sheet 覆盖完整 `960 × 1600px` 画布，浅色节目面内使用 `848px` 内容列 |
| 原型坐标 | Radio 04/05/06 使用 VDA-17 固定骨架：Radio rail `x 72 / w 816`，输入框 `x 72 / y 1372 / w 816 / h 88`，Tab Bar `x 170 / y 1480 / w 620 / h 88` |
| 安全边距 | 原型页面左右使用 `56px`、顶部使用 `40px` 安全边距；底部预留固定导航和安全区 |
| 底部导航 | 一级页面使用固定底部胶囊式 Tab Bar，`620 × 88px`，5 个 `124 × 88px` tab slot；选中态使用白色圆角矩形底和纯黑图标，提供 Tooltip、`aria-label` 和键盘 Focus |
| 桌面 standalone PWA | 以 `960 × 1600px` 画布按实际可视区域等比缩小，窄窗口和浏览器缩放下仍完整展示页面且外层不滚动；普通浏览器与手机保留响应式布局 |
| Radio 输入操作 | 语音与发送按钮在同一布局族中必须使用相同的圆角正方形尺寸；Desktop `64 × 64px`，Mobile `48 × 48px` |
| 按钮颜色 | 主按钮使用黑白高对比，不使用绿色填充；绿色只用于 `ON AIR`、`LIVE`、`PLAYING`、`CONNECTED`、Focus Ring、细线和少量波形 |
| 字体 | 正文使用系统字体；等宽字体仅用于时间、数字、状态标签和队列序号；正文和 DJ 串讲不得使用点阵字体 |
| 页面结构 | Radio 始终保持中央窄列，不做桌面多栏；管理页保持单列，不做后台式侧边栏 |
| DJ 对话区 | Radio 主页面展开态使用 `816 × 288px` 独立 dialogue well，Queue 折叠态平滑扩展以回收空间；DJ 文案用自然段，用户输入用右对齐弱气泡且与 DJ 正文使用同一字号和行高 |
| Detail Sheet | 覆盖完整产品界面，不露出底层 Radio 页面；使用深色声波区、纯白节目面 `#FFFFFF` 和浅灰串讲词/歌词卡片 `#F5F3F6`；歌词卡片可滚动但不显示滚动条，当前歌词显著放大并自动居中；不显示专辑封面、上一首、下一首、喜欢按钮或更多按钮 |
| 卡片使用 | 不把所有区块都做成悬浮卡片；Radio 中仅播放器、队列、输入区等需要边界的模块卡片化 |
| 错误提示 | 非阻断反馈可用 Toast；阻断错误必须在相关区块内联显示，并提供重试、跳转 Settings、修改输入或降级继续播放入口 |
| 动效 | 动效只表达声音、状态和空间关系；避免持续装饰动画、大幅弹跳、高频闪烁和横向页面滑动 |
| 无障碍 | 所有正式组件支持键盘操作、可见 Focus Ring、WCAG AA 对比度、`aria-live` 重要状态播报、200% 浏览器缩放和 `prefers-reduced-motion` |

## 二、页面结构

### 1. 导航结构

| 导航层级 | 页面名称 | 页面定位 | 入口 | 主要承载功能 |
|----------|----------|----------|------|--------------|
| 异常态 | 本地服务未连接 | 本地服务不可用时的恢复入口 | 打开产品且服务未响应 | 重连、只读 Settings、查看脱敏诊断与启动说明 |
| 一级 | Onboarding/Profile | 首次进入、档案选择和档案创建 | 首次打开、顶部头像、登录/档案入口 | 创建本地档案、选择档案、基础偏好录入 |
| 一级 | Radio 主播放页 | 核心电台体验入口 | 默认打开、底部胶囊导航 Radio | 场景输入、DJ 串讲、播放控制、队列、反馈 |
| 一级 | Library | 音乐库和歌单来源管理 | 底部胶囊导航 Library、Radio 队列空状态入口 | 搜索歌曲、导入歌单、查看候选歌曲 |
| 一级 | Taste | 个人音乐品味档案 | 底部胶囊导航 Taste、Profile 完成后入口 | 查看和编辑风格标签、喜欢/避雷规则、场景偏好 |
| 一级 | Programs/History | 节目单与播放历史 | 底部胶囊导航 Programs、Radio 播放完成入口 | 查看历史节目、重播串讲、复用场景 |
| 一级 | Settings | 本地服务、设备配置、数据目录与 Profile 偏好 | 底部胶囊导航 Settings、异常提示入口 | Codex 命令路径、内置网易云 Provider 健康状态、可选 TTS、受控数据目录迁移、主题和 DJ 偏好 |
| 二级详情态 | Radio Detail Sheet | 当前节目沉浸详情 | 用户点击 Radio 页 DJ 状态栏 | 波形、AI 串讲词、歌词、歌曲进度、节目整体进度、单一播放/暂停 |

### 2. 页面职责说明

| 页面名称 | 用户目标 | 关键内容/组件 | 空状态 | 异常状态 |
|----------|----------|----------------|--------|----------|
| 本地服务未连接 | 恢复本地服务连接 | `○ OFFLINE`、断开波纹状态图标、重新连接、只读 Settings 入口、`LOCAL SERVICE · NOT RESPONDING` | 本页面为空状态不适用 | 仅已打开或已缓存的 PWA 可进入只读 Settings；所有配置、测试与迁移控件禁用，只显示启动说明、脱敏状态和重试 |
| Onboarding/Profile | 创建或选择本地电台档案 | 头像、昵称、电台名、常听风格、默认场景、档案列表 | “还没有电台档案，创建你的私人电台” | 本地数据目录不可写时提示检查权限 |
| Radio 主播放页 | 输入场景并完成电台播放 | 单列固定顺序：顶部品牌区、时间/ON AIR、播放器、队列、DJ 状态栏、DJ 对话区、底部输入；主状态词使用英文 | `Say something to the DJ...` | 本地服务断开时显示“Koradio 服务未连接” |
| Library | 准备音乐候选来源 | 搜索框、歌单导入、歌曲列表、加入候选池按钮 | “还没有导入歌单，可先搜索一首歌试播” | 网易云 API 不可用时提示切换关键词或重试 |
| Taste | 管理个人音乐品味 | 品味标签、避雷规则、场景规则、反馈摘要、编辑按钮 | “播放和反馈后会在这里形成你的音乐品味” | 档案读取失败时提示重新选择档案 |
| Programs/History | 回看和复用历史节目 | 节目卡片、播放记录、DJ 文案、重播按钮、复用场景按钮 | “还没有节目，去 Radio 生成第一段电台” | 历史音频文件缺失时保留文本并提示无法重播音频 |
| Settings | 配置本地运行依赖和当前 Profile 偏好 | 本地服务状态、Codex 路径、内置网易云 Provider 状态、Qwen3-TTS 模型状态与下载进度、数据目录、主题模式、DJ 语言、DJ 声音风格、测试与迁移入口 | “完成 Codex 配置并确认音乐服务可用后即可开始播放” | Provider、模型下载或 helper 超时、迁移或配置保存失败时显示具体原因；完全离线时只读 |
| Radio Detail Sheet | 沉浸查看当前节目 | 全屏沉浸节目界面、顶部状态、全宽动态波形、纯白节目面 `#FFFFFF`、节目标题、简洁当前歌曲、歌曲进度、`#F5F3F6` 串讲词/歌词卡片、节目整体进度、关闭按钮；DJ 段串讲为主，歌曲段歌词为主 | `No live session yet` | 歌词缺失、TTS 失败、播放失败时显示降级内容 |

### 3. Radio 主播放页布局规则

Radio 主播放页采用单列固定顺序，不因主题模式变化而改变信息架构。首屏优先建立电台仪式感，完整露出顶部品牌区、时间/日期、电台状态和播放器；队列、DJ 状态栏、DJ 对话区和底部输入从下方自然延续。空节目状态显示 `LIVE`，节目准备完成或正在播放时显示 `ON AIR`，生成中显示 `TUNING`。

高保真生图原型中的 04/05/06 三态必须共用同一套固定骨架：

| 固定区域 | 原型坐标与尺寸 | 说明 |
|----------|----------------|------|
| 顶部品牌工具区 | `x 56 / y 40 / w 848 / h 64` | Logo、`KORADIO` 字标、头像、主题按钮 |
| Radio 内容 rail | `x 72 / w 816` | 时间、播放器/空态/生成态、队列、DJ 状态栏、DJ 对话区、输入框 |
| 时间状态区 | `x 72 / y 128 / w 816 / h 148` | 时间、日期、`LIVE/ON AIR/TUNING/OFFLINE` |
| 状态主内容区 | `x 72 / y 292 / w 816 / h 340` | 空状态、播放器或生成中核心内容 |
| 队列区 | `x 72 / y 656 / w 816 / h 300` | 空队列、真实队列或骨架队列 |
| DJ 状态栏 | `x 72 / y 980 / w 816 / h 64` | 点击打开 Detail Sheet |
| DJ 对话区 | `x 72 / y 1068 / w 816 / h 288` | 独立 dialogue well，DJ 自然段和用户弱气泡 |
| 底部输入 | `x 72 / y 1372 / w 816 / h 88` | 输入、语音和发送 |
| 底部导航 | `x 170 / y 1480 / w 620 / h 88` | 5 个等宽图标 Tab |

| 顺序 | 区块 | 组件细节 | 交互规则 |
|------|------|----------|----------|
| 1 | 顶部品牌区 | 头像、`KORADIO` 品牌字标、Profile 入口、`Dark/Light` 快捷切换 | 用户点击头像进入 Profile；点击主题切换即时预览并保存 |
| 2 | 时间/电台状态 | 大号点阵时间、星期、日期、状态点、`LIVE/ON AIR/TUNING/OFFLINE` | 状态随本地服务连接、节目生成和播放状态更新 |
| 3 | 播放器 | 当前歌曲、歌手、音量、上一首、播放/暂停、下一首、队列、喜欢歌曲、More、进度条 | 控制 Browser Audio Engine 中的播放时间线并和 Detail Sheet 同步；不喜欢位于 More，节目收藏仅位于 Programs/节目入口 |
| 4 | 队列 | `QUEUE`、曲目数、当前高亮曲目、前 3-5 首列表、`HIDE/LIST` | 空状态只显示状态栏；生成节目后默认展开 3-5 首 |
| 5 | DJ 状态栏 | `Koradio`、`LIVE/SPEAKING/THINKING/PLAYING`、状态灯 | 用户点击后打开全屏 Radio Detail Sheet |
| 6 | DJ 对话区 | `816 × 288px` dialogue well、DJ 自然段、用户弱气泡、时间、`REPLAY` | 展示开场和关键转场，不承担长篇聊天记录，不做聊天机器人界面 |
| 7 | 底部输入 | `Say something to the DJ...`、语音按钮、发送按钮、连接状态 | 用户发送场景后进入 `THINKING`，生成期间禁用重复发送 |

### 4. 页面状态矩阵

以下页面状态用于对齐 `design.md` 与高保真原型。实现时可复用路由和组件，但用户可见状态必须区分。

| 编号 | 页面/状态 | 内容宽度 | 必须出现的核心内容 |
|------|-----------|----------|--------------------|
| 01 | 本地服务未连接 | `560px` | `○ OFFLINE`、标题“Koradio 服务未连接”、重新连接、只读 Settings、脱敏诊断与启动说明 |
| 02 | 本地档案选择 | `848px` | 品牌字标、档案卡片、`CURRENT`、创建档案、本地存储说明 |
| 03 | 创建电台档案 | `848px` | 头像上传、电台名称、昵称、常听风格、默认场景、保存并进入 Koradio |
| 04 | Radio 空节目 | `816px` | `● LIVE`、`NOW PLAYING`、`NO SESSION ON AIR`、`QUEUE · 0 TRACKS`、`816 × 288px` dialogue well、场景输入 |
| 05 | Radio 正在播放 | `816px` | `● ON AIR`、`816 × 340px` 当前播放器、队列 3-5 首、当前曲目绿色细线、DJ 自然段串讲、用户弱气泡 |
| 06 | Radio 生成中 | `816px` | `TUNING`、`THINKING`、`TUNING YOUR STATION...`、队列骨架、用户弱气泡、禁用输入框 |
| 07 | Detail Sheet：DJ 串讲 | `960 × 1600px` 全屏覆盖 | `● SPEAKING NOW`、拖动条、关闭按钮、深色声波区、纯白节目面、节目标题、`If · Bread`、歌曲进度、`848 × 700px` 串讲词卡片、节目整体进度、单一播放/暂停 |
| 08 | Detail Sheet：歌词跟随 | `960 × 1600px` 全屏覆盖 | `● PLAYING`、拖动条、关闭按钮、深色声波区、纯白节目面、节目标题、`Space Song · Beach House`、歌曲进度、`848 × 700px` 歌词卡片、当前歌词高亮、节目整体进度、单一播放/暂停 |
| 09 | Library 音乐库 | `840px` | 标题、搜索框、搜索结果、试听、加入候选池、导入网易云歌单、服务状态 |
| 10 | Taste 品味档案 | `840px` | 品味摘要、编辑入口、常听风格、喜欢的声音、避雷规则、场景偏好、最近反馈 |
| 11 | Taste 编辑状态 | `840px` | 返回、标签编辑、避雷规则、场景规则、取消、保存品味、数量限制 |
| 12 | Programs 节目历史列表 | `840px` | 本周摘要、节目卡片、收藏、场景摘要、曲目数量、总时长 |
| 13 | 节目历史详情 | `840px` | `PROGRAM ARCHIVE`、节目元数据、`YOUR SCENE`、复用场景、重播串讲、节目队列 |
| 14 | Settings 服务配置 | `832px` | 服务状态、Codex 命令路径、内置网易云 Provider 只读状态、Qwen3-TTS 模型状态与下载进度、Theme Mode、DJ Language、DJ Voice Style、本地数据 |
| 15 | Settings 连接检测结果 | `832px` | `3 OF 4 SERVICES AVAILABLE`、四张检测结果卡片、Qwen3-TTS 修复建议、返回 Radio、修改配置 |

### 5. 页面与功能映射

| 功能名称 | 入口页面 | 过程页面 | 结果展示页面 |
|----------|----------|----------|--------------|
| 服务配置与健康检查 | 本地服务未连接、Settings | 本地服务未连接、Settings | Settings、Radio 主播放页 |
| 本地档案创建与选择 | Onboarding/Profile | Onboarding/Profile | Radio 主播放页、Taste |
| 场景点歌电台生成 | Radio 主播放页 | Radio 主播放页 | Radio 主播放页、Radio Detail Sheet |
| 播放控制与队列管理 | Radio 主播放页 | Radio 主播放页 | Radio 主播放页 |
| 沉浸节目界面 | Radio 主播放页 | Radio Detail Sheet | Radio Detail Sheet |
| 音乐库搜索与导入 | Library | Library | Library、Radio 主播放页 |
| 品味档案查看与编辑 | Taste | Taste | Taste、Radio 主播放页 |
| 播放历史与节目复用 | Programs/History | Programs/History | Radio 主播放页 |

## 三、功能列表

| 模块 | 功能名称 | 功能说明 | 优先级 |
|------|----------|----------|--------|
| 档案 | 本地档案创建与选择 | 用户可创建、选择、编辑本地电台档案，隔离品味和历史 | P0 |
| 电台 | 场景点歌电台生成 | 用户输入场景后，系统通过 Codex 生成节目计划、搜索歌曲、生成 DJ 串讲和队列 | P0 |
| 播放 | 播放控制与队列管理 | 用户可播放、暂停、切歌、喜欢歌曲、通过 More 标记不喜欢，以及查看和折叠队列 | P0 |
| 电台 | 沉浸节目界面 | 用户点击 DJ 状态栏后查看波形、串讲词和歌词跟随 | P0 |
| 记忆 | 反馈与品味沉淀 | 用户喜欢/取消喜欢、不喜欢/取消不喜欢、收藏/取消收藏节目或跳过后，系统追加事件并更新品味投影 | P0 |
| 配置 | 服务配置与健康检查 | 用户配置设备级 Codex 和数据目录，配置当前 Profile 的主题、DJ 语言与声音风格，并查看内置网易云 Provider、Qwen3-TTS runtime 与模型状态 | P0 |
| 音乐库 | 音乐库搜索与歌单导入 | 用户可搜索歌曲、导入网易云歌单并加入候选池 | P1 |
| 品味 | 品味档案查看与编辑 | 用户可查看和编辑风格标签、避雷规则和场景偏好 | P1 |
| 历史 | 节目历史与复用 | 用户可查看历史节目、重播 DJ 串讲、复用历史场景 | P1 |

## 四、功能说明

### 功能 1：本地档案创建与选择

**功能描述**

用户可在 Onboarding/Profile 页面创建或选择本地电台档案，隔离各 Profile 的品味、历史与展示偏好；设备级服务配置在所有 Profile 间共享。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击“创建电台档案” → 系统打开档案表单 → 用户看到头像、电台名、昵称、常听风格、默认场景输入项。
- 用户点击“保存并进入 Koradio” → 系统创建 `Profile`、默认 `TasteOverrides` 与 `ProfilePreferences` → 用户看到 Radio 主播放页进入空播放状态。
- 用户点击已有档案卡片 → 系统加载该档案的 `EffectiveTaste`、节目历史和 `ProfilePreferences`，并读取共享 `DeviceSettings` → 用户看到对应档案的 Radio 页和头像。
- 正在生成或播放时切换 Profile → 取消旧 Profile 的生成任务并丢弃其迟到事件，保存旧播放 checkpoint、停止旧时间线，再加载新 Profile。
- 数据变化：新增或更新 `Profile`、`TasteOverrides`、`ProfilePreferences`；当前 Profile ID 只写入本机 bootstrap 运行配置。

**用户流程**

```text
[首次打开 Koradio]
  ↓
[检查是否存在本地档案]
  ├─ 无档案 → 用户填写档案表单
  │          ├─ 保存成功 → 创建 Profile + TasteOverrides + ProfilePreferences → 进入 Radio
  │          └─ 保存失败 → 提示本地目录不可写或字段错误
  └─ 有档案 → 展示档案列表
             ├─ 用户选择档案 → 加载档案 → 进入 Radio
             └─ 加载失败 → 提示重新选择或检查本地数据
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未初始化 | 首次打开且无本地档案 | 用户创建档案成功 | 显示创建档案引导 |
| 选择档案 | 检测到已有本地档案 | 用户选择档案成功 | 显示档案卡片列表 |
| 编辑中 | 用户点击创建或编辑 | 保存成功或取消 | 显示档案表单 |
| 加载中 | 用户选择档案 | 加载成功或失败 | 按钮显示“正在进入电台...” |
| 已进入 | 档案加载成功 | 用户切换档案或退出 | 进入 Radio 主播放页 |
| 错误 | 本地目录不可写或档案损坏 | 用户重试或更换路径 | 显示错误提示和重试入口 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 电台名 | 文本 | 是 | 2-24 个字符 | 不允许纯空格 | “请输入 2-24 个字符的电台名” |
| 昵称 | 文本 | 是 | 1-20 个字符 | 不允许纯空格 | “请输入你的昵称” |
| 头像 | 上传图片 | 否 | 由上传接口限制 | 上传成功后只保存受控 `avatarRef`；拒绝任意 URL、绝对路径和裸文件名 | “头像上传失败，请重新选择图片” |
| 常听风格 | 标签数组 | 否 | 最多 12 个标签 | 每个标签 1-24 个字符 | “每个风格标签需在 24 个字符内” |
| 默认场景 | 文本 | 否 | 120 个字符 | 可为空 | “默认场景不能超过 120 个字符” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 还没有电台档案，创建你的私人 Koradio |
| 加载中 | 正在进入你的电台... |
| 操作成功 | 电台档案已创建 |
| 操作失败 | 档案保存失败，请检查输入内容 |
| 权限不足 | 当前数据目录不可写，请在 Settings 中发起受控迁移 |
| 网络异常 | 本功能使用本地数据，不依赖网络；如仍失败，请检查本地服务状态 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 本地目录不可写 | 保存 profile 失败 | 阻止保存，保留表单内容 | “当前数据目录不可写，请更换路径后重试” |
| 档案损坏 | profile JSON/DB 读取失败 | 跳过损坏档案，允许重新选择 | “该档案无法读取，可切换档案或检查本地文件” |
| 字段为空 | 必填字段为空 | 表单内联报错 | “请输入电台名和昵称” |
| 操作超时 | 本地服务 5 秒未响应 | 展示重试按钮 | “Koradio 服务响应超时，请重试” |

### 功能 2：场景点歌电台生成

**功能描述**

用户可在 Radio 主播放页底部输入框描述当前场景，系统通过本地 Codex 编排生成节目计划、DJ 串讲、候选歌曲和播放队列，解决用户手动找歌和拼歌单的问题。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户在输入框输入“今晚写东西，想要安静但不死板的 BGM” → 系统校验输入并锁定发送按钮 → 用户看到 DJ 状态栏变为 `THINKING`。
- 用户点击发送按钮 → 系统读取 `EffectiveTaste`、节目历史、当前时间、`ProfilePreferences`、共享 `DeviceSettings`，并通过 Library application API 获取当前 Profile 最多 500 首可播放 Live 音乐库摘要、`maximumTracks` 和建议库内数量，请求本地 Codex 输出节目计划 → 用户看到 DJ 对话区出现 `Tuning your station...`。
- Codex 生成串讲 → 系统按英式电台主持人规则控制语气、长度和说话频率 → 用户看到一段克制、有场景感的开场，而不是每首歌前都被打断。
- Codex 返回有序 `trackIntents` → 系统按顺序解析库内 ID，或为每个探索关键词最多选择一首非库内歌曲，再获取播放链接和歌词 → 用户看到队列逐步生成。默认五首节目建议四首库内、一首探索；明确语言、地区、艺术家、“只听库内”或“只探索新歌”的请求优先。
- TTS 可用且生成成功 → 系统将带音频的 `dj` 项插入 `PlaybackTimeline` → 用户听到 DJ 开场并看到串讲文本；TTS 缺失或失败时，文字串讲只保留在 Program segment 中，不创建伪音频项。
- 当前已有节目时，旧节目在新节目生成期间继续播放；生成失败时保持旧节目不变；新节目完整提交后保存旧 checkpoint、停止旧时间线并原子切换。
- 数据变化：新增 `Program`、`DjScriptSegment`、`MusicTrack` 与已提交的 `PlaybackTimeline`；场景输入在提交前只属于前端 draft，不写入持久模型。

**用户流程**

```text
[Radio 底部输入框]
  ↓
[用户输入场景并发送]
  ├─ 输入为空 → 提示输入场景，不发起请求
  └─ 输入有效 → 构建上下文
       ↓
     [请求本地 Codex 生成节目计划]
       ├─ 成功 → 按有序 library/discovery intent 解析
       │          ├─ 至少一首可播放 → 获取播放链接/歌词 → 可选生成 TTS → 原子提交新节目 → 开始播放
       │          └─ 全部 intent 失效 → 保留旧节目并提示换个描述
       ├─ Codex 失败 → 保留用户输入并提示重试
       └─ 超时 60 秒 → 中断规划并提示重试
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 空闲 | Radio 已连接且无生成任务 | 用户发送场景 | 输入框可编辑，DJ 状态为 `LIVE` |
| 校验失败 | 输入为空或超过限制 | 用户修改输入 | 输入框下方显示错误 |
| 规划中 | 用户发送有效场景 | Codex 返回、失败或超时 | DJ 状态为 `THINKING`，发送按钮禁用 |
| 搜歌中 | Codex 返回候选策略 | 搜歌成功或失败 | 队列区显示骨架项 |
| 合成中 | 歌曲候选可用且有 DJ 文案 | TTS 成功或失败 | DJ 状态保持 `THINKING`；TTS 段实际播放时 Detail Sheet 显示 `SPEAKING` |
| 可提交 | 队列和至少一首歌曲可用 | 原子提交成功或失败 | 旧节目继续播放；提交成功后一次性切换 |
| 播放中 | 音频开始播放 | 暂停、结束或失败 | 播放按钮切换为暂停 |
| 失败 | Codex、搜歌或播放失败 | 用户重试或修改输入 | 显示错误提示和重试入口 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 场景输入 | 文本 | 是 | 1-500 个字符 | 去除首尾空格后不可为空 | “告诉 DJ 你现在想听什么” |
| 语音输入转写文本 | 文本 | 否 | 500 个字符 | 转写失败时不覆盖原输入 | “没有听清，可以再说一次” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | Say something to the DJ... |
| 加载中 | Tuning your station... |
| 操作成功 | Your session is on air |
| 操作失败 | 这次没有规划成功，请稍后重试 |
| 权限不足 | 请先在 Settings 配置 Codex |
| 网络异常 | 音乐服务连接失败，请检查网络或稍后重试 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| Codex 失败 | Codex 返回错误或非结构化结果 | 保留用户输入和上下文，允许重试 | “Codex 没有完成节目规划，点击重试” |
| Codex 超时 | 60 秒未返回 | 中断请求，释放输入框 | “规划时间太久了，请重试或缩短描述” |
| Track intent 无结果或不可播 | 库内 ID 不属于当前 Profile、不可播放，或探索搜索失败/无合格结果 | 按 intent 顺序跳过并发布受控降级；Discovery 每个关键词最多选一首，稳定去重，不随机跨 Profile 补位；全部失效时保留旧节目 | “没有找到合适歌曲，换个说法试试” |
| TTS 失败 | TTS 45 秒超时或返回错误 | 保留文字 DJ，跳过语音串讲 | “语音合成失败，本次先用文字 DJ” |
| 播放链接失效 | 歌曲 URL 不可播放 | 跳过该曲并尝试下一首 | “这首暂时不能播放，已切到下一首” |
| 缺少核心配置 | 缺少 Codex 配置 | 阻止生成并跳转 Settings | “请先完成 Codex 配置” |
| Qwen3-TTS 不可用 | 设备不支持、模型未安装或损坏、helper 不可用或能力检查失败 | 直接使用完整文字 DJ，不阻止生成 | “本地语音不可用，本次使用文字 DJ” |

### 功能 3：播放控制与队列管理

**功能描述**

用户可在 Radio 主播放页控制当前节目播放，查看和管理队列，解决生成节目后无法主动调整播放节奏的问题。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击播放/暂停按钮 → 系统暂停或恢复当前 TTS/歌曲音频 → 用户看到按钮状态、进度条和 DJ 状态同步变化。
- 用户点击下一首 → 系统结束当前音频并切换到队列下一项 → 用户看到当前歌曲、进度条、歌词和队列高亮更新。
- 用户点击心形按钮 → 系统追加“喜欢歌曲”事件；用户通过 More 标记不喜欢；节目收藏只在 Programs/节目入口处理。
- 系统生成节目成功 → 系统默认展开节目中的全部歌曲 → 用户看到准确的队列总数，并能确认当前高亮曲目；固定高度内最多显示 4 行，更多歌曲可在不显示滚动条的列表内滚动查看。
- 用户点击 `HIDE/LIST` → 系统折叠或展开队列区 → 用户看到队列列表隐藏或显示；折叠时 DJ 状态栏平滑上移、DJ 对话区同步上移并增高，相邻区域保持 `24px` 间距，输入框和底部导航位置不变。
- 用户拖动进度条 → 系统 seek 到目标时间 → 用户看到串讲高亮或歌词高亮同步跳转。
- 运行时变化：当前 index、音量、进度、队列展开态、Detail 开关和高亮行都由 Browser Audio Engine 或页面 UI state 管理。仅在切歌、暂停、Profile 切换、标签接管和节目切换等边界保存带当前 `leaseEpoch` 的低频 `PlaybackCheckpoint`；服务端拒绝低于已保存 epoch 的旧标签页写入，跳过通过 `track_skipped` 事件记录。
- checkpoint 的 `positionMs` 不得超过目标 timeline item 时长；只有位于最后一个 item 且精确到达该 item 末尾时才能保存 `completed`，并在同一事务中把 Program 标记为 `completed`。

**用户流程**

```text
[Radio 播放器区域]
  ↓
[用户操作播放控件]
  ├─ 播放/暂停 → 更新 Browser Audio Engine → UI 同步状态
  ├─ 下一首/上一首 → 切换 queue index → 加载对应音频
  ├─ 拖动进度 → seek 音频 → 同步串讲/歌词高亮
  ├─ 生成后队列 → 默认展开 3-5 首
  ├─ 折叠队列 → 仅更新当前标签页 UI state → DJ 区平滑补位
  └─ 播放失败 → 尝试下一首或提示重试
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 无队列 | 尚未生成节目 | 生成节目成功 | 只显示 `QUEUE` 状态栏和 `0 TRACKS` |
| 队列展开 | 节目生成成功 | 用户点击 `HIDE` | 展示节目全部歌曲和准确总数；超过 4 首时列表内部滚动但不显示滚动条，当前曲目高亮 |
| 队列折叠 | 用户点击 `HIDE` | 用户点击 `LIST` | 只显示 `QUEUE` 状态栏和曲目数量；DJ 状态栏与对话区回收列表空间，对话区扩大且各区间距保持 `24px` |
| 已就绪 | 队列已生成且音频可用 | 用户点击播放 | 显示当前歌曲和可播放按钮 |
| 播放中 | 音频播放开始 | 用户暂停、切歌、播放结束 | 按钮显示暂停，进度条推进 |
| 暂停 | 用户点击暂停 | 用户点击播放或切歌 | 按钮显示播放，进度停止 |
| 切换中 | 用户点击上一首/下一首 | 新音频加载成功或失败 | 当前歌曲区域显示加载状态 |
| 播放失败 | 音频加载或播放失败 | 自动跳过或用户重试 | 显示失败提示 |

**字段规范**

本功能无用户输入字段。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 还没有播放队列，先告诉 DJ 你想听什么 |
| 加载中 | 正在加载下一段声音... |
| 操作成功 | 已切换播放 |
| 操作失败 | 当前音频无法播放 |
| 权限不足 | 当前档案不可写，播放记录不会保存 |
| 网络异常 | 播放链接连接失败，正在尝试下一首 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 播放失败 | HTMLAudio 报错或 URL 失效 | 自动跳过到下一首，可手动重试 | “当前音频无法播放，已尝试下一首” |
| 队列为空 | 用户点击播放但无队列 | 引导输入场景 | “先告诉 DJ 你想听什么” |
| 进度越界 | seek 时间超过音频时长 | 修正到合法范围 | 进度条回到最近可用位置 |
| 本地状态保存失败 | 写入播放历史失败 | 继续播放，提示历史未保存 | “播放继续，但历史记录保存失败” |

### 功能 4：沉浸节目界面

**功能描述**

用户可点击 Radio 主播放页中部的 DJ 状态栏，打开全屏沉浸节目界面，集中查看音乐播放波形、AI 串讲词和歌词跟随，解决主页面信息分散、不够沉浸的问题。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击 `Koradio LIVE` DJ 状态栏 → 系统打开覆盖完整产品界面的 Radio Detail Sheet → 用户看到深色声波区、纯白节目面、当前节目标题、简洁歌曲信息、歌曲进度、串讲词/歌词卡片、节目整体进度和关闭按钮。
- 用户播放到 DJ 串讲段落 → 系统将串讲词切为主内容并按句高亮 → 用户看到当前句变亮、已读句变淡、歌词区域降为辅助。
- 用户播放到歌曲段落 → 系统将歌词切为主内容并按 LRC 行高亮 → 用户看到普通歌词以 `clamp(24px, 3.75vw, 36px)` 展示、当前歌词以 `clamp(34px, 5vw, 48px)` 高字重展示并随播放自动滚动到卡片中央；歌词卡片保留键盘滚动且不显示可见滚动条，首尾内容均可到达。
- 用户点击 Detail Sheet 内唯一的播放/暂停按钮 → 系统控制同一条全局播放时间线 → 用户看到主播放器和 Detail Sheet 状态同步。
- 用户点击关闭按钮或拖动顶部控制条收起 → 系统收起详情态但不中断播放 → 用户回到 Radio 主播放页。
- Detail Sheet 内不显示专辑封面、上一首、下一首、喜欢按钮、更多按钮或传统底部播放器控制台；主播放页是否保留切歌控制不受本规则影响。
- 状态变化：详情覆盖层开关属于页面 UI state；当前时间线项、串讲句和歌词行均由 Browser Audio Engine 的实时进度派生，不写入持久模型。

**用户流程**

```text
[Radio DJ 状态栏]
  ↓
[用户点击状态栏]
  ├─ 当前有节目 → 打开全屏沉浸节目界面
  │      ├─ 播放 DJ 段 → 串讲词为主 + 分句高亮 + 歌词辅助
  │      ├─ 播放歌曲段 → 歌词为主 + 逐行高亮 + 串讲辅助
  │      ├─ 歌词缺失 → 显示无歌词文案
  │      └─ 用户关闭 → 回到 Radio，播放不中断
  └─ 当前无节目 → 提示先生成电台
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 关闭 | 默认状态 | 用户点击 DJ 状态栏 | 只显示 Radio 主播放页 |
| 打开中 | 用户点击状态栏 | 动画完成 | Detail Sheet 覆盖完整产品画布，显示深色声波区和纯白节目面 |
| DJ 跟随 | 当前时间线为 TTS 段 | 时间线进入歌曲段或关闭 | 串讲词作为主内容逐句高亮，歌词降为辅助 |
| 歌词跟随 | 当前时间线为歌曲段 | 时间线进入 DJ 段或关闭 | 歌词作为主内容逐行高亮，串讲降为辅助 |
| 降级显示 | 缺少歌词、TTS 或波形数据 | 数据恢复或关闭 | 显示文字 DJ、无歌词提示、模拟波形 |
| 关闭中 | 用户点击关闭或拖动顶部控制条 | 动画结束 | 返回 Radio 主播放页 |

**字段规范**

本功能无用户输入字段。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | No live session yet |
| 加载中 | Opening live session... |
| 操作成功 | Live session opened |
| 操作失败 | 无法打开节目详情 |
| 权限不足 | 当前档案不可读取，无法显示节目详情 |
| 网络异常 | 歌词加载失败，播放不受影响 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 没有当前节目 | 队列为空时点击状态栏 | 不打开卡片，引导生成节目 | “先生成一段电台，再查看节目详情” |
| 歌词缺失 | 网易云歌词为空或失败 | 显示歌曲信息和无歌词状态 | “暂无歌词，正在播放 DJ 推荐曲目” |
| TTS 时间戳缺失 | TTS 不返回分句时间 | 按文本长度估算高亮 | 串讲仍可读，当前句近似高亮 |
| LRC 时间轴缺失 | 歌词存在但无逐行时间 | 显示静态歌词，不做逐行高亮 | “Lyrics available, live timing unavailable” |
| 波形不可用 | 无真实 waveform peaks | 使用前端模拟动态柱状图 | 波形继续显示但不代表真实频谱 |
| 界面渲染失败 | 前端组件报错 | 回退到 Radio 主页面 | “节目详情暂时不可用，播放继续” |

### 功能 5：反馈与品味沉淀

**功能描述**

用户可对歌曲执行喜欢、不喜欢、撤销与跳过，也可在 Programs/节目入口收藏或取消收藏节目。系统以 append-only 事件保存每次意图，并据此重建自动品味投影。全屏 Detail Sheet 只承载沉浸阅读和播放/暂停，不放置反馈控件。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击 Radio 心形按钮 → 系统追加 `track_liked`；再次取消时追加 `track_like_removed`，不覆盖旧事件。
- 用户从 More 选择不喜欢 → 系统追加 `track_disliked`；撤销时追加 `track_dislike_removed`。
- 用户跳过歌曲 → Browser Audio Engine 切到下一项并追加 `track_skipped`。
- 用户在 Programs/节目入口收藏或取消收藏 → 分别追加 `program_favorited` 或 `program_favorite_removed`。
- 数据变化：只新增 `FeedbackEvent`；`TasteProjection` 和界面反馈态由事件流重建，人工 `TasteOverrides` 不被覆盖。
- 自动投影按数据库分配的稳定追加顺序回放每个 Profile 的完整事件流：歌曲喜欢/不喜欢与节目收藏分别按对应事件对的最新有效状态计算，removed 事件只撤销对应状态；`track_skipped` 保留为可追溯事实并推进 projection 版本，但 v1 不把单次跳过推断为不喜欢。
- v1 不从反馈目标猜测风格标签；自动 `tags` 保持为空。有效歌曲喜欢生成 `track:<targetId>` affinity，有效歌曲不喜欢生成同格式 avoid signal，有效节目收藏生成 `program:<targetId>` affinity。
- `sourceVersion` 等于该 Profile 已成功持久化的去重事件数；重复 idempotency key 返回原事件，不推进版本。重建使用全部事件重新计算同一结果，不修改 `FeedbackEvent` 或 `TasteOverrides`。

**用户流程**

```text
[Radio、More 或 Programs]
  ↓
[用户触发反馈]
  ├─ 喜欢/撤销 → 追加 track_liked / track_like_removed → 更新投影
  ├─ 不喜欢/撤销 → 追加 track_disliked / track_dislike_removed → 更新投影
  ├─ 跳过 → 切到下一首 → 追加 track_skipped
  └─ 收藏/取消收藏节目 → 追加 program_favorited / program_favorite_removed
       └─ 保存失败 → 播放继续，提示反馈未保存
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未反馈 | 当前歌曲未被操作 | 用户点击反馈按钮 | 喜欢按钮为空心 |
| 正反馈 | 用户点击喜欢 | 用户取消喜欢或切歌 | 喜欢按钮点亮 |
| 负反馈 | 用户点击不喜欢 | 切歌或撤销 | 显示“已减少类似推荐” |
| 已收藏 | 用户在 Programs 收藏节目 | 用户取消收藏 | 节目收藏按钮点亮 |
| 保存中 | 用户触发反馈 | 保存成功或失败 | 按钮短暂 loading |
| 保存失败 | 本地写入失败 | 用户重试或忽略 | 显示非阻塞提示 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 事件类型 | 枚举 | 是 | 固定枚举 | `track_liked`、`track_like_removed`、`track_disliked`、`track_dislike_removed`、`program_favorited`、`program_favorite_removed`、`track_skipped` | “反馈类型无效” |
| 反馈对象 ID | 文本 | 是 | 128 个字符 | 必须对应当前歌曲或节目 | “当前内容无法反馈” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 播放后可以告诉 Koradio 喜不喜欢 |
| 加载中 | 正在记录你的反馈... |
| 操作成功 | 已记住你的偏好 |
| 操作失败 | 反馈保存失败，请重试 |
| 权限不足 | 当前档案不可写，无法保存反馈 |
| 网络异常 | 反馈保存在本地，网络异常不影响记录 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 无当前歌曲 | 队列为空时点击反馈 | 阻止操作 | “当前没有可反馈的歌曲” |
| 重复点击 | 短时间连续点击同一反馈 | 每次已确认状态变化都追加显式事件；重复的同状态命令按幂等键去重 | 按钮状态由最新有效事件投影 |
| 本地写入失败 | DB/JSON 写入失败 | 保留 UI 状态 3 秒后回滚 | “反馈保存失败，已恢复原状态” |
| 档案不可写 | 数据目录权限不足 | 引导 Settings | “请检查本地数据目录权限” |

### 功能 6：服务配置与健康检查

**功能描述**

用户可在 Settings 页面管理设备级 `DeviceSettings`（数据目录和 Codex）以及当前档案的 `ProfilePreferences`（主题、DJ 语言和声音风格），并查看内置网易云 Provider 与 Qwen3-TTS 的运行时健康和模型安装状态。首次启动自动使用 OS 应用数据目录，不要求用户先选择路径。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户打开 Koradio 但本地服务未响应 → 已打开或缓存的 PWA 显示 `○ OFFLINE`；只读 Settings 禁用全部配置、测试与迁移控件，仅显示本地服务启动说明、脱敏的上次状态和重试。未缓存的 PWA 不承诺离线可达。
- 用户点击 Settings → 系统分别读取共享 `DeviceSettings`、当前 `ProfilePreferences` 和运行时 `ServiceHealth` 快照。
- 用户填写 Codex 命令路径并点击“保存配置” → 普通配置写入 `DeviceSettings` → 用户看到“配置已保存”；内置网易云 Provider 和 Qwen3-TTS 都不保存用户地址或密钥。
- 用户点击“下载本地语音模型” → 系统从固定 revision 下载约 1.84 GiB 模型并显示进度 → 校验完成后原子启用；用户离开页面不终止下载，应用退出会安全中止并清理 partial。
- 用户选择 `Theme Mode` 为 `Dark`、`Light` 或 `System` → 系统立即预览主题并保存偏好 → 用户看到 Radio 主播放页和详情页按对应昼夜视觉基准切换。
- 用户选择 `DJ Language` 为中文或英文 → 系统保存串讲语言偏好 → 用户下一次生成节目时听到对应语言的 DJ 串讲。
- 用户选择 `DJ Voice Style` 为 `Natural Radio` → 系统将该声音风格传给 Codex 编排上下文；中文通过 `Serena`、英文通过 `Ryan` 合成自然电台口吻，模型不可用时降级为完整文字 DJ。
- 用户点击“测试连接” → 系统分别检查 Codex、内置网易云 Provider、Qwen runtime 与模型状态 → 用户看到每项成功或失败；网易云和 Qwen3-TTS 不出现凭据或 `NOT CONFIGURED`。
- 用户查看服务检测结果 → 系统汇总可用服务数量并展开失败项修复建议 → 用户看到 `3 OF 4 SERVICES AVAILABLE`、TTS 降级说明、返回 Radio 和修改配置按钮。
- 用户在 Settings 发起数据目录迁移 → 系统创建幂等异步迁移任务：验证目标为空且可写，暂停生成和播放，保存 checkpoint，备份并复制校验，原子切换 bootstrap 配置后重启服务；失败时回滚到旧目录，旧数据永不自动删除。
- 数据变化：设备配置写入 `DeviceSettings`，档案偏好写入 `ProfilePreferences`；网易云配置不持久化，`ServiceHealth` 仅是运行时快照。原始 Codex 输出和未脱敏诊断不得进入播放历史或普通日志。

**用户流程**

```text
[首次启动或 Settings 页面]
  ↓
[首次启动自动选择 OS 应用数据目录；用户查看服务状态]
  ├─ 本地服务未连接 → 显示 OFFLINE 异常页 → 重试或进入只读 Settings
  ├─ Codex 与网易云可用 → Radio 可生成节目
  │      ├─ Qwen3-TTS 模型就绪 → 中文 Serena / 英文 Ryan 生成语音 DJ
  │      └─ 原生 helper、匹配语音缺失或合成失败 → 文字 DJ 降级
  ├─ 用户切换 Theme Mode → 立即预览昼夜主题 → 保存偏好
  ├─ 用户切换 DJ Language → 下次生成节目使用对应语言
  ├─ 用户切换 DJ Voice Style → 下次 TTS 使用对应声音风格
  ├─ 用户发起数据目录迁移 → 异步执行阶段并发布进度 → 成功重启或失败回滚
  └─ Codex 配置缺失 → 用户填写配置并保存
         ├─ 保存成功 → 可测试连接
         └─ 保存失败 → 提示本地配置不可写
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未配置 | Codex 配置缺失 | 用户保存完整 Codex 配置 | Codex 配置显示未完成，网易云与 Qwen3-TTS 单独显示能力状态 |
| 服务未连接 | 打开产品时本地服务未响应 | 重连成功 | 已缓存 PWA 显示 `○ OFFLINE`、只读 Settings、启动说明和脱敏诊断 |
| 已配置 | Codex 配置存在 | 用户修改 Codex 配置 | Codex 配置显示已填写，网易云与 Qwen3-TTS 独立显示可用或降级 |
| 检测中 | 用户点击测试连接 | 检测完成或超时 | 每项显示检测中 |
| 检测结果 | 服务检测完成 | 用户返回 Radio 或修改配置 | 显示可用服务数量、失败项和修复建议 |
| 可用 | 服务测试成功 | 下次检测失败 | 状态点为绿色 |
| 部分不可用 | 任一服务失败 | 用户修复并检测成功 | 失败项标红，显示原因 |
| 主题预览 | 用户切换 Theme Mode | 保存成功或取消 | Radio 预览切换为 Dark/Light/System |
| 语言已切换 | 用户切换 DJ Language | 下次生成节目 | 设置项显示中文或 English |
| 保存失败 | 配置写入失败 | 用户重试或更换路径 | 显示保存错误 |
| 迁移中 | 用户发起合法的数据目录迁移 | 成功重启或失败回滚 | 显示验证、暂停、checkpoint、备份、复制校验、切换、重启阶段 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| Codex 命令路径 | 文本 | 是 | 300 个字符 | 本地路径或可执行命令名 | “请输入可用的 Codex 命令路径” |
| 本地数据路径 | 本地路径 | 是 | 300 个字符 | 首次启动由 OS 应用数据目录提供；迁移目标必须为空且可写 | “请选择空且可写的数据目录” |
| Theme Mode | 枚举 | 是 | 固定枚举 | `dark`、`light`、`system`，默认 `dark` | “请选择主题模式” |
| DJ Language | 枚举 | 是 | 固定枚举 | `zh-CN`、`en-GB`，默认 `zh-CN` | “请选择 DJ 串讲语言” |
| DJ Voice Style | 枚举 | 是 | 固定枚举 | 默认 `natural-radio` | “请选择 DJ 声音风格” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 完成必要配置后即可开始电台 |
| 加载中 | 正在检测服务连接... |
| 操作成功 | 配置已保存 |
| 操作失败 | 配置保存失败，请检查输入 |
| 权限不足 | 当前数据路径不可写 |
| 网络异常 | 外部服务连接失败，请检查网络后重试 |
| 主题切换 | 主题已切换 |
| 语言切换 | DJ 串讲语言将在下一次节目生效 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| Codex 不可用 | 命令不存在或返回错误 | 阻止场景生成 | “Codex 未连接，请检查命令路径” |
| 内置网易云 Provider 不可用 | 健康检查失败或 15 秒超时 | 标记运行时不可用并允许重试，不展示地址或密钥表单 | “网易云服务暂不可用，无法搜索歌曲” |
| Qwen3-TTS 不可用 | 设备不支持、模型缺失或损坏、helper 缺失、合成错误或 45 秒超时 | 允许完整文字 DJ 降级，不调用云 TTS | “本地语音不可用，将仅显示文字串讲” |
| Qwen 模型下载失败 | 网络、磁盘空间或 SHA-256 校验失败 | 删除应用拥有的 partial，保留未安装状态并允许重试 | “模型下载失败，可稍后重试；文字 DJ 不受影响” |
| 模型下载时迁移数据目录 | 用户在下载过程中发起迁移 | 返回冲突并保持现有目录 | “请等待模型下载完成后再迁移数据目录” |
| 数据路径不可写 | 保存配置或历史失败 | 阻止相关写入 | “请选择可写的数据目录” |
| 数据目录迁移失败 | 目标验证、复制校验、原子切换或重启失败 | 回滚 bootstrap 配置并恢复旧目录；旧数据保留 | “迁移未完成，已继续使用原数据目录” |
| 完全离线 | Local Service 不可达 | 禁用配置、测试和迁移控件，只允许重试与查看启动说明/脱敏状态 | “本地服务未启动，设置暂时只读” |
| 主题保存失败 | themeMode 写入失败 | 回滚到上一次主题 | “主题保存失败，已恢复上一次设置” |
| DJ 语言无效 | 设置值不在枚举内 | 阻止保存并恢复默认中文 | “DJ 语言设置无效，已恢复中文” |

**多标签播放所有权**

- 同一设备与 Koradio origin 使用 `BroadcastChannel + localStorage TTL lease` 选出唯一主控标签；租约携带当前 `profileId`，主控每 `2s` 续约，租约 `5s` 未续约即过期。
- 被动标签只读展示低频状态，可申请接管，但不得启动第二条音频时间线或提交播放控制。
- 主动交接时，原主控先保存 checkpoint 并停止播放，再释放租约；异常过期后的新主控从最近 checkpoint 恢复。
- 所有命令携带 lease epoch；Local Service 和标签页均丢弃旧 epoch 的迟到控制事件。

### 功能 7：音乐库搜索与歌单导入

**功能描述**

用户可在 Library 页面搜索歌曲、导入网易云歌单并加入候选池，为后续 Codex 策展提供更贴近个人品味的音乐来源。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户在 Library 搜索框输入歌曲或歌手 → 系统调用网易云 API 搜索 → 用户看到歌曲列表、歌手、专辑和试听按钮。
- 用户点击“加入候选池” → 系统保存歌曲元数据和来源 → 用户看到歌曲出现在本地音乐库。
- 用户输入网易云歌单链接并点击导入 → 系统解析完整曲目 ID 清单并分批拉取歌曲 → 用户看到总数、导入进度和结果。
- 完整曲目 ID 清单按每批最多 100 首补齐详情；Provider 全部读取成功后才在单个数据库事务中写入，失败时不得留下半成功导入。
- 导入中持续展示 `processed / total`；完成后展示 `imported / unavailable`。重复导入同一来源更新元数据并补充新增歌曲，不重复创建已有歌曲。
- Library 列表展示“已显示 n / 总计 m”，总计来自完整候选池而非当前分页；Live 模式另行隔离 Demo 数量。
- 单曲试听必须提供加载、播放、停止、失败和重试状态；错误显示在对应歌曲行，不以全局无响应代替。
- 数据变化：新增或更新 `MusicTrack`、`PlaylistSource`、`LibraryItem`；歌曲保存专辑封面与可播放状态，并将导入来源作为下一次 `TasteProjection` 重建输入。

**用户流程**

```text
[Library 页面]
  ↓
[用户搜索或导入歌单]
  ├─ 搜索歌曲 → 网易云返回结果 → 加入候选池
  ├─ 导入歌单 → 解析歌单 ID → 拉取歌曲 → 写入本地库
  ├─ 无结果 → 提示换关键词
  └─ API 失败 → 保留输入并允许重试
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 空库 | 本地库无歌曲 | 用户加入歌曲或导入歌单 | 显示导入引导 |
| 搜索中 | 用户提交关键词 | 返回结果或失败 | 搜索框显示 loading |
| 有结果 | 网易云返回歌曲 | 用户清空搜索或切页 | 显示歌曲列表 |
| 导入中 | 用户提交歌单链接 | 导入成功或失败 | 显示导入进度 |
| 已导入 | 歌单写入完成 | 用户删除来源 | 显示导入数量 |
| 失败 | 搜索或导入失败 | 用户重试 | 显示失败原因 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 搜索关键词 | 文本 | 否 | 100 个字符 | 搜索时不可为空 | “请输入歌曲、歌手或专辑名” |
| 网易云歌单链接 | URL/文本 | 否 | 300 个字符 | 必须能解析出歌单 ID | “请输入有效的网易云歌单链接或 ID” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 还没有导入歌单，可先搜索一首歌试播 |
| 加载中 | 正在从网易云获取音乐... |
| 操作成功 | 已加入本地音乐库 |
| 操作失败 | 音乐获取失败，请重试 |
| 权限不足 | 内置网易云服务当前不可用，请前往 Settings 查看状态 |
| 网络异常 | 网易云连接失败，请稍后重试 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 搜索无结果 | 网易云返回空数组 | 展示空状态和换词建议 | “没有找到相关歌曲，换个关键词试试” |
| 歌单解析失败 | 链接无合法 ID | 阻止导入 | “无法识别歌单链接” |
| 部分歌曲不可用 | 歌单内歌曲缺少播放链接 | 保留全部可解析歌曲，标记不可播放项并禁用试听/播放 | “已导入歌曲，部分歌曲暂不可播放” |
| API 限制或失败 | 网易云返回错误 | 保留输入，允许重试 | “网易云 API 暂不可用” |

### 功能 8：品味档案查看与编辑

**功能描述**

用户可在 Taste 页面查看由系统自动推导并与人工规则合并的个人音乐品味，使 Codex 后续推荐有明确且可控的偏好依据。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击 Taste → 系统读取自动 `TasteProjection`、人工 `TasteOverrides` 和合并后的 `EffectiveTaste` → 用户看到系统推断、人工规则和最近反馈摘要。
- 用户点击“编辑标签” → 系统只编辑 `TasteOverrides` → 用户可新增、删除、排序人工标签与规则。
- 用户点击“保存品味” → 系统写入 `TasteOverrides` 并重新计算 `EffectiveTaste` → 用户看到“已更新你的音乐品味”。
- 自动投影可由完整 `FeedbackEvent` 流重建；重建、回放或算法升级不得覆盖人工 overrides，冲突时人工规则优先。
- 合并使用稳定、保序、忽略大小写的去重：人工 tags、避雷规则和场景规则完整保留在各自字段前部；自动 tags 和 signals 只填充 contract 剩余容量。
- 人工避雷规则与自动 tag 或 affinity 文本相同时移除对应自动值；自动 avoid signal 在人工避雷规则之后填充剩余容量。`EffectiveTaste` 是实时合并的只读结果，不单独持久化或提供写入口。

**用户流程**

```text
[Taste 页面]
  ↓
[用户查看品味档案]
  ├─ 有数据 → 展示标签/规则/反馈摘要
  ├─ 无数据 → 展示播放和反馈引导
  └─ 用户编辑 → 保存 TasteOverrides → 合并 EffectiveTaste
       ├─ 成功 → 后续场景生成使用新偏好
       └─ 失败 → 保留编辑内容并提示重试
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未形成 | 无播放历史和反馈 | 用户播放并反馈 | 显示引导文案 |
| 已形成 | 存在标签或反馈摘要 | 用户进入编辑 | 显示品味卡片 |
| 编辑中 | 用户点击编辑 | 保存或取消 | 显示可编辑标签和规则 |
| 保存中 | 用户点击保存 | 保存成功或失败 | 保存按钮 loading |
| 已更新 | 保存成功 | 用户继续编辑或离开 | 显示成功提示 |
| 保存失败 | 写入失败 | 用户重试或取消 | 显示错误提示 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 风格标签 | 标签数组 | 否 | 最多 30 个标签 | 每个标签 1-24 个字符 | “每个标签需在 24 个字符内” |
| 避雷规则 | 文本数组 | 否 | 最多 20 条，每条 120 字 | 不允许纯空格 | “避雷规则不能超过 120 个字符” |
| 场景规则 | 文本数组 | 否 | 最多 20 条，每条 160 字 | 不允许纯空格 | “场景规则不能超过 160 个字符” |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 播放和反馈后会在这里形成你的音乐品味 |
| 加载中 | 正在读取你的品味档案... |
| 操作成功 | 已更新你的音乐品味 |
| 操作失败 | 品味保存失败，请重试 |
| 权限不足 | 当前档案不可写，无法更新品味 |
| 网络异常 | 本功能使用本地数据，网络异常不影响编辑 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 档案为空 | 无标签和历史 | 展示引导，不阻断页面 | “先生成几次电台，让 Koradio 认识你” |
| 标签重复 | 用户新增重复标签 | 合并重复项 | “已合并重复标签” |
| 保存失败 | 本地写入失败 | 保留编辑态 | “保存失败，内容已保留” |
| 规则过长 | 超过字段限制 | 阻止保存并定位字段 | 展示对应字段错误 |

### 功能 9：节目历史与复用

**功能描述**

用户可在 Programs/History 页面查看历史节目、重播 DJ 串讲、复用历史场景重新生成节目，解决好节目难以找回和复用的问题。

**用户操作路径、界面元素、交互逻辑、数据变化**

- 用户点击 Programs/History → 系统读取历史节目列表 → 用户看到节目标题、场景输入、歌曲数量、生成时间和收藏状态。
- 用户点击节目卡片 → 系统展示节目详情，包括 DJ 文案、歌曲队列、反馈结果 → 用户可查看当时的推荐理由。
- 用户点击“复用场景” → 系统把历史场景填入 Radio 输入框并可重新生成 → 用户看到 Radio 页进入待发送状态。
- 用户点击“重播串讲” → 系统播放已缓存 TTS 或回退到文字 → 用户听到或看到历史 DJ 串讲。
- 数据变化：读取 `Program`；复用时只将历史场景填入当前标签页的场景 draft，用户确认提交后才创建新 `Program`。

**用户流程**

```text
[Programs/History 页面]
  ↓
[用户查看历史节目]
  ├─ 有历史 → 打开节目详情
  │      ├─ 重播串讲 → 播放缓存 TTS 或显示文字
  │      ├─ 复用场景 → 跳转 Radio 并填入历史场景
  │      └─ 收藏/取消收藏 → 追加 program_favorited / program_favorite_removed
  └─ 无历史 → 引导前往 Radio
```

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 无历史 | 没有 program 记录 | 生成第一个节目 | 显示空状态 |
| 列表 | 存在节目历史 | 用户打开详情 | 显示节目卡片 |
| 详情 | 用户点击节目 | 用户返回列表 | 显示 DJ 文案和队列 |
| 重播中 | 用户点击重播串讲 | 播放结束或失败 | 显示重播进度 |
| 复用中 | 用户点击复用场景 | 跳转 Radio | Radio 输入框填入历史场景 |
| 错误 | 历史读取失败 | 用户重试 | 显示错误提示 |

**字段规范**

本功能无用户输入字段。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 还没有节目，去 Radio 生成第一段电台 |
| 加载中 | 正在读取节目历史... |
| 操作成功 | 已带着这个场景回到 Radio |
| 操作失败 | 节目历史读取失败 |
| 权限不足 | 当前档案不可读取 |
| 网络异常 | 历史保存在本地，网络异常不影响查看 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 历史为空 | 无 program 记录 | 展示空状态和 Radio 入口 | “去 Radio 生成第一段电台” |
| TTS 缓存缺失 | 历史音频文件被删除 | 显示文字串讲，不播放音频 | “串讲音频缺失，已显示文字版” |
| 历史读取失败 | 本地数据损坏 | 提供重试和回到 Radio | “节目历史暂时无法读取” |
| 复用失败 | Radio 状态不可用 | 保留在详情页并提示 | “Radio 未连接，暂时不能复用场景” |

## 五、接口与数据约束

### 1. 本地服务能力

| 能力 | 说明 | 最小输入 | 最小输出 | 失败降级 |
|------|------|----------|----------|----------|
| 档案管理 | 创建、读取、选择、更新本地档案 | profile 字段 | 当前 profile | 本地目录不可写时阻止保存 |
| 场景规划 | 调用本地 Codex 生成节目计划 | 场景文本、taste、history、时间、偏好、最多 500 首库内摘要与曲目上限 | 含有序 `trackIntents` 的结构化 program plan | Codex 失败时保留输入并提示重试 |
| 意图解析与音乐搜索 | 通过 Library application API 解析库内 ID，或调用网易云 API 搜索探索歌曲和播放链接 | 有序 library/discovery intent | 稳定去重的歌曲元数据、播放 URL | 单 intent 失败时跳过；全部失败时保留旧节目 |
| TTS 生成 | 调用持久化 bundled Python/MLX helper，以固定 Qwen3-TTS 8-bit 模型生成完整 WAV | 完整 DJ 文案、语言、声音风格 | 受控音频引用、时长；无 marker 时估算时间 | 失败时保留完整文字 DJ |
| 时间线构建 | 将节目转换为可播放判别联合并确定顺序 | program plan | `PlaybackTimelineItem[]` | 单曲失败时 Browser Audio Engine 跳到下一项 |
| 播放 checkpoint | 在状态边界保存可恢复的低频播放快照 | program、timeline item、position、`leaseEpoch` | `PlaybackCheckpoint` | 保存失败不影响当前 Browser Audio Engine 播放；旧 epoch 写入被拒绝 |
| 历史记录 | 保存节目、播放、反馈 | program、track、feedback | history record | 保存失败时提示但不中断播放 |
| 数据目录迁移 | 以幂等异步任务迁移设备数据根目录 | 目标目录、idempotency key | `jobId`、阶段事件 | 失败回滚，旧数据不自动删除 |
| WebSocket 推送 | 推送领域变化、任务阶段和低频 snapshot | 当前连接 | 统一 event envelope | 不推送高频播放进度；断开时前端显示未连接并尝试重连 |

### 2. 最小数据对象

| 对象 | 必需字段 | 用途 |
|------|----------|------|
| Profile | id、radioName、nickname、avatarRef、createdAt、updatedAt | 区分本地用户档案；`avatarRef` 必须由受控上传生成 |
| TasteProjection | profileId、tags、affinities、avoidSignals、sourceVersion、updatedAt | 由反馈与历史自动重建的品味投影 |
| TasteOverrides | profileId、tags、avoidRules、sceneRules、updatedAt | 人工编辑的规则，优先级高于自动投影 |
| EffectiveTaste | profileId、projectionVersion、overrideVersion、resolvedTaste | 提供给 Codex 的合并只读视图，不单独成为写入入口 |
| MusicTrack | id、source、sourceTrackId、title、artist、album、duration、lyricStatus、originMode | 保存稳定歌曲元数据；`lyricStatus` 支持 `unknown/available/untimed/unavailable`，短期播放 URL 属于运行时解析结果 |
| PlaylistSource | id、profileId、source、sourcePlaylistId、title、originMode | 保存歌单来源、导入统计和 Live/Demo 来源 |
| program | id、profileId、scenarioText、title、status、trackIds、originMode、createdAt | 保存一次电台节目 |
| CurrentProgram | profileId、programId?、updatedAt | 保存每个 Profile 的当前节目指针；空指针不得回退到历史第一条 |
| DjScriptSegment | id、programId、type、language、text、displayText、estimatedTiming、ttsAudioRef? | 保存开场、转场、结束语；文字降级仍保留在这里 |
| PlaybackTimelineItem | `dj`：id、segmentId、audioRef、duration；或 `track`：id、trackId、resolvedAudioRef、duration | 有音频的判别联合；文字 DJ 不创建伪 item |
| PlaybackCheckpoint | profileId、programId、timelineItemId、positionMs、volume、status、leaseEpoch、savedAt | 每个 Profile 只保留最新低频恢复快照；实时进度不持久化，旧 lease 不得覆盖新状态 |
| FeedbackEvent | id、profileId、targetId、type、idempotencyKey、createdAt | append-only 显式反馈事件，type 使用功能 5 的固定枚举 |
| DeviceSettings | dataRoot、codexCommand、updatedAt | 设备级共享配置；v1 的内置网易云 Provider 和 Qwen3-TTS 不保存用户地址或密钥，模型状态由受控文件系统推导 |
| ProfilePreferences | profileId、themeMode、djLanguage、djVoiceStyle、updatedAt | Profile 级展示与 DJ 偏好 |
| ServiceHealth | service、status、checkedAt、redactedSummary | 运行时快照，不是配置或历史事实 |

### 3. 配置接口与异步事件

| 接口 | 语义 |
|------|------|
| `GET/PATCH /api/v1/device-settings` | 读取或更新设备级非敏感配置；v1 不接受网易云地址、Cookie 或密钥字段 |
| `GET /api/v1/device-settings/tts-model` | 读取固定 Qwen 模型的支持、安装、下载进度和脱敏错误状态 |
| `POST /api/v1/device-settings/tts-model/install` | 明确触发固定 revision 模型首次下载；已就绪时幂等返回，设备不支持时返回冲突 |
| `GET/POST /api/v1/profiles` | 列出或创建本地档案；创建命令要求 `Idempotency-Key`，并原子创建默认 `TasteOverrides` 与 `ProfilePreferences` |
| `GET/PATCH /api/v1/profiles/:profileId` | 读取或更新显式指定的本地档案；v1 不提供删除接口 |
| `POST /api/v1/profile-avatars` | 接收单个 `multipart/form-data` 头像文件，校验真实图片类型、大小与受控文件策略后返回 `avatarRef` |
| `GET/PUT /api/v1/profiles/current` | 读取或切换当前 Profile；切换命令显式携带 `profileId`，并按取消旧生成、丢弃迟到事件、保存并停止旧播放的顺序协调 |
| `GET/PATCH /api/v1/profiles/:profileId/preferences` | 读取或更新指定 Profile 的主题与 DJ 偏好 |
| `GET/PATCH /api/v1/profiles/:profileId/taste` | 读取 projection/overrides/effective，或只更新人工 TasteOverrides |
| `POST /api/v1/profiles/:profileId/feedback-events` | 使用 `Idempotency-Key` 追加固定枚举 FeedbackEvent；成功后更新 projection 并发布 `feedback.persisted` |
| `GET /api/v1/profiles/:profileId/programs` | 分页读取指定 Profile 的 Program 历史摘要；详情字段按需加载 |
| `GET /api/v1/profiles/:profileId/programs/current` | 读取当前节目指针对应的完整节目；空指针返回空结果，不从历史推断 |
| `GET /api/v1/profiles/:profileId/programs/:programId` | 读取指定 Profile 拥有的 Program、DJ segments、歌曲元数据和判别式 timeline 详情 |
| `DELETE /api/v1/profiles/:profileId/programs/:programId` | 永久删除节目；当前节目同时停止播放并清空指针，返回音频删除、保留和待清理数量 |
| `GET /api/v1/profiles/:profileId/playback` | 读取指定 Profile 最新的低频 Playback checkpoint 与对应 timeline item；无 checkpoint 返回未找到 |
| `PUT /api/v1/profiles/:profileId/playback/checkpoints` | 保存显式携带 `leaseEpoch` 的低频 checkpoint；校验 Program/timeline ownership、位置边界和完成边界 |
| `POST /api/v1/device-settings/data-root-migrations` | 以 idempotency key 创建数据目录迁移，立即返回 `202 { jobId }`；重复命令返回同一任务；模型下载中返回冲突 |

迁移阶段通过既有 WebSocket event envelope 发布，包含 `eventId`、`eventType`、`version`、`occurredAt`、可选 `profileId`、`correlationId`、`sequence` 和 `payload`。迁移事件类型为 `data_root_migration.stage_changed`，payload 中包含 `jobId`、`stage`、`status` 与脱敏错误码；不得包含路径中的用户名、密钥或文件正文。

### 4. Codex 结构化输出要求

Codex 作为本地编排大脑时，应输出可解析 JSON。MVP 最小结构如下：

```json
{
  "programTitle": "Monday Night Exhale",
  "scenarioSummary": "夜晚写作，需要安静但不死板的 BGM",
  "djLanguage": "zh-CN",
  "djPersona": "natural-radio",
  "djScripts": [
    {
      "type": "intro",
      "language": "zh-CN",
      "text": "今晚适合慢一点，但不要睡着。我先给你挑几首有呼吸感的歌。",
      "displayText": "今晚适合慢一点，但不要睡着。我先给你挑几首有呼吸感的歌。",
      "estimatedTiming": true
    }
  ],
  "trackIntents": [
    {
      "kind": "library",
      "trackId": "00000000-0000-4000-8000-000000000001",
      "reason": "音乐库中的温暖人声与轻柔律动锚点"
    },
    {
      "kind": "discovery",
      "keyword": "If Bread",
      "reason": "与库内原声 Soft Rock 锚点相邻的探索歌曲"
    }
  ],
  "playlistIntent": {
    "energy": "low-mid",
    "mood": "calm",
    "avoid": ["过强鼓点", "高亢人声"]
  }
}
```

Codex 输出必须遵守以下规则：

- 根据 `ProfilePreferences.djLanguage` 输出中文或英文串讲；中文串讲可保留少量 `ON AIR`、`Now Playing` 等电台状态词。
- 根据 `ProfilePreferences.djVoiceStyle = natural-radio` 控制文案：柔和、有磁性、专业克制、带轻微冷幽默。
- 每次节目至少输出 `intro`；仅在风格明显切换、用户追问、关键歌曲或结束时输出 `segue` 或 `outro`。
- 不输出每首歌固定口播，不编造无法确认的音乐背景；事实不确定时只描述歌曲氛围、听感和用户场景。
- `displayText` 为旧数据和接口兼容字段；新节目提交时必须与 `text` 逐字一致。TTS、Radio、Detail 和节目历史统一使用完整 `text`。
- TTS 无分句时间戳时，`estimatedTiming` 必须为 `true`，前端按文本长度进行近似高亮。
- `trackIntents` 必须是播放顺序；`library` intent 只能引用 planning context 中存在的当前 Profile 曲目 ID，`discovery` intent 每个关键词只代表一个探索位置。
- 默认按建议库内数量规划约 70% 库内、30% 探索，五首节目为四首库内、一首探索；明确语言、地区、艺术家、“只听库内”或“只探索新歌”的请求优先于默认比例。
- 库内 intent 非法、重复、不可播放或探索搜索失败时只做受控跳过，不得随机附加当前 Profile 之外的歌曲填满队列。

解析失败时，本地服务不得保存或回显原始正文。日志只记录稳定错误码、correlation ID、schema 失败摘要和脱敏诊断元数据；前端统一收到“Codex 没有完成节目规划，点击重试”，不可解析内容不得进入节目、历史或日志正文。

### 3.1 历史节目删除

- 节目卡片和详情菜单提供“删除节目”，二次确认中展示节目标题和“不可恢复”说明；删除失败时卡片保持可见。
- 删除当前节目时停止播放、释放多标签播放租约、清空 CurrentProgram，并持久回到 Radio 无节目状态。
- 删除事务清理 Program、ProgramTrack、DJ 段、时间线、checkpoint 和对应成功生成任务。收藏节目先追加 `program_favorite_removed`，反馈历史保持 append-only，并同步重建 TasteProjection。
- 仅删除未被其他节目引用的 TTS 文件。文件先移入受控暂存区，数据库失败时恢复；提交后物理删除。最终清理失败进入受控待清理记录并在启动时重试，接口返回警告。
- 成功删除发布 `program.deleted`，其他标签页停止同一节目并刷新 Radio、Programs、Favorites 和本周统计。
